---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;

const errors = [
  {
    title: 'Depender solo de ChatGPT gratuito',
    description:
      'La versión gratis tiene límites importantes. Hay tareas donde Claude es mejor para análisis profundo, Perplexity para búsqueda legal actualizada, y Gemini para procesamiento de datos. No hay una sola herramienta perfecta.',
    icon: 'cancel',
  },
  {
    title: 'Usar prompts genéricos',
    description:
      '"Ayúdame con este caso" no funciona. Un prompt efectivo necesita: Rol + Contexto + Tarea + Formato. Sin estructura, obtienes respuestas vagas.',
    icon: 'cancel',
  },
  {
    title: 'Creer que la IA te reemplazará',
    description:
      'La IA no sustituye tu criterio profesional. Es una herramienta que multiplica tu capacidad de análisis, no tu reemplazo.',
    icon: 'cancel',
  },
];
---

<section
  class={`relative py-20 lg:py-32 px-4 md:px-10 overflow-hidden font-display ${className}`}
>
  <!-- Decorative background blur -->
  <div
    class="absolute top-0 right-0 -mr-20 -mt-20 w-[400px] h-[400px] bg-primary/5 rounded-full blur-[100px] pointer-events-none -z-10"
  >
  </div>
  <div
    class="absolute bottom-0 left-0 -ml-20 -mb-20 w-[300px] h-[300px] bg-secondary/5 rounded-full blur-[80px] pointer-events-none -z-10"
  >
  </div>

  <div class="max-w-[1100px] mx-auto flex flex-col gap-16 relative z-10">
    <!-- Headline -->
    <div
      class="flex flex-col items-center text-center gap-4 animate-on-scroll"
      data-animation="fade-in-up"
    >
      <span
        class="inline-block px-3 py-1 rounded-full bg-primary/10 text-primary text-[10px] font-black uppercase tracking-widest border border-primary/10"
      >
        La Realidad de la Industria
      </span>
      <h2
        class="text-text-main tracking-tight text-3xl md:text-[42px] font-extrabold leading-[1.15] max-w-3xl"
      >
        ¿Por qué compartir esto sin costo?
      </h2>
      <p class="text-text-muted text-lg max-w-2xl leading-relaxed font-medium">
        Llevo 12 años como contador y he entrenado a más de 500 contadores en el
        uso de IA. He visto los mismos errores repetirse una y otra vez.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      <!-- Left Column: Pain Points -->
      <div class="flex flex-col gap-8 order-2 lg:order-1">
        <div
          class="flex flex-col gap-2 animate-on-scroll"
          data-animation="fade-in-up"
        >
          <h3 class="text-text-main text-xl font-bold">
            Errores comunes que te frenan
          </h3>
          <p class="text-sm text-text-muted font-medium">
            Lo que la mayoría hace mal al intentar digitalizarse.
          </p>
        </div>

        <div class="flex flex-col gap-4">
          {
            errors.map((error, index) => (
              <div
                class="group flex gap-4 rounded-2xl border border-slate-100 bg-white p-5 shadow-sm hover:shadow-md transition-all duration-300 animate-on-scroll"
                data-animation="fade-in-up"
                data-delay={index * 100}
              >
                <div class="flex-shrink-0 mt-1">
                  <span class="material-symbols-outlined text-red-500 bg-red-50 p-2 rounded-xl group-hover:scale-110 transition-transform">
                    {error.icon}
                  </span>
                </div>
                <div class="flex flex-col gap-1">
                  <h4 class="text-text-main text-base font-extrabold leading-tight group-hover:text-red-600 transition-colors">
                    {error.title}
                  </h4>
                  <p class="text-text-muted text-sm font-medium leading-relaxed">
                    {error.description}
                  </p>
                </div>
              </div>
            ))
          }
        </div>
      </div>

      <!-- Right Column: Mission Statement -->
      <div class="flex flex-col justify-center order-1 lg:order-2">
        <div
          class="relative rounded-3xl bg-white p-8 md:p-12 border border-slate-50 shadow-2xl shadow-slate-200/50 animate-on-scroll"
          data-animation="scale-in"
        >
          <!-- Decorative quote icon -->
          <div
            class="absolute -top-6 -left-4 md:-left-8 bg-primary text-white p-3 rounded-2xl shadow-xl rotate-3"
          >
            <span class="material-symbols-outlined !text-4xl leading-none"
              >format_quote</span
            >
          </div>

          <div class="flex flex-col gap-6 relative z-10">
            <h3
              class="text-text-main text-2xl md:text-[28px] font-extrabold leading-snug tracking-tight"
            >
              Mi objetivo es simple.
            </h3>
            <p
              class="text-lg md:text-xl text-slate-600 leading-relaxed font-semibold"
            >
              Que veas <span
                class="text-primary font-black bg-primary/5 px-1.5 rounded-lg"
                >casos reales</span
              > resueltos paso a paso para que decidas por ti mismo si este enfoque
              tiene sentido para tu práctica.
            </p>
            <p class="text-base text-text-muted leading-relaxed font-medium">
              Si te funciona, tengo un programa más profundo. Si no, al menos te
              llevas 3 casos prácticos con prompts que puedes usar desde mañana.
            </p>

            <div
              class="flex items-center gap-4 mt-4 pt-6 border-t border-slate-100"
            >
              <div
                class="h-14 w-14 rounded-2xl bg-gradient-to-br from-primary to-indigo-600 flex items-center justify-center text-white font-black text-xl shadow-lg shadow-primary/20 rotate-3"
              >
                IC
              </div>
              <div class="flex flex-col">
                <span class="text-text-main font-black">Israel Castro</span>
                <span
                  class="text-[10px] text-text-muted font-black uppercase tracking-widest opacity-70"
                  >Contador Público | +500 contadores capacitados en IA</span
                >
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
