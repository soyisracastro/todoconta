---
interface Props {
  registrationUrl: string;
  deadline: string;
}

const { registrationUrl, deadline } = Astro.props;
---

<section
  class="relative flex flex-col items-center justify-center py-24 lg:py-32 overflow-hidden bg-[#f8faff] dark:bg-[#0b0f1a]"
>
  <!-- Subtle Background Decor -->
  <div
    class="absolute inset-0 bg-gradient-to-b from-blue-50/50 to-white dark:from-blue-900/10 dark:to-[#0b0f1a] pointer-events-none"
  >
  </div>

  <div class="container mx-auto px-4 relative z-10 flex flex-col items-center">
    <div class="max-w-4xl w-full flex flex-col items-center text-center">
      <!-- Headline Group -->
      <div
        class="flex flex-col gap-6 text-center items-center max-w-[850px] animate-on-scroll"
        data-animation="fade-in-up"
      >
        <h2
          class="text-[#111827] dark:text-white tracking-tight text-4xl md:text-5xl lg:text-7xl font-black leading-[1.05] font-display m-0"
        >
          No dejes pasar esta oportunidad
        </h2>
        <div class="flex flex-col gap-2">
          <p
            class="text-[#4b5563] dark:text-gray-400 text-lg md:text-2xl font-medium leading-relaxed max-w-[700px] m-0"
          >
            En 12 meses habrá dos tipos de contadores: los que dominan IA y los
            que compiten por precio.
          </p>
          <p
            class="text-lg md:text-2xl font-bold text-[#2563EB] dark:text-blue-400 m-0 font-display"
          >
            ¿De qué lado quieres estar?
          </p>
        </div>
      </div>

      <!-- CTA Button Area -->
      <div
        class="flex flex-col items-center justify-center w-full mt-12 gap-6 animate-on-scroll"
        data-animation="scale-in"
        data-delay="200"
      >
        <!-- Main Blue Button -->
        <a
          href={registrationUrl}
          target="_blank"
          class="group relative flex min-w-[280px] sm:min-w-[360px] cursor-pointer items-center justify-center rounded-xl h-18 px-10 bg-[#2563EB] text-white text-xl font-bold leading-none tracking-tight transition-all duration-300 transform hover:-translate-y-1 shadow-[0_15px_30px_-5px_rgba(37,99,235,0.4)] hover:shadow-[0_25px_40px_-10px_rgba(37,99,235,0.5)] no-underline font-display"
        >
          <span class="mr-3 relative z-10">Reservar mi lugar ahora</span>
          <span
            class="material-symbols-outlined relative z-10 group-hover:translate-x-1 transition-transform duration-300 font-bold"
            >arrow_forward</span
          >
        </a>

        <!-- Date Info -->
        <div class="flex items-center gap-2 mt-2">
          <span
            aria-hidden="true"
            class="material-symbols-outlined text-[#2563EB] text-[20px]"
            >calendar_today</span
          >
          <p
            class="text-[#6b7280] dark:text-gray-400 text-sm font-semibold tracking-tight m-0"
          >
            Inicia el {deadline}
          </p>
        </div>
      </div>

      <!-- Centered Trust Badges -->
      <div
        class="mt-24 flex flex-wrap justify-center gap-x-12 gap-y-6 w-full max-w-2xl border-t border-gray-100 dark:border-white/5 pt-12 animate-on-scroll"
        data-animation="fade-in-up"
        data-delay="400"
      >
        <div class="flex items-center gap-2.5">
          <span
            class="material-symbols-outlined text-gray-400 dark:text-gray-500 text-[22px]"
            style="font-variation-settings: 'FILL' 1;"
          >
            verified_user
          </span>
          <span
            class="text-[11px] font-black text-gray-500 dark:text-gray-400 tracking-[0.12em] uppercase font-display"
          >
            Pago 100% Seguro
          </span>
        </div>
        <div class="flex items-center gap-2.5">
          <span
            class="material-symbols-outlined text-gray-400 dark:text-gray-500 text-[22px]"
            style="font-variation-settings: 'FILL' 1;"
          >
            school
          </span>
          <span
            class="text-[11px] font-black text-gray-500 dark:text-gray-400 tracking-[0.12em] uppercase font-display"
          >
            Certificado Incluido
          </span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .font-display {
    font-family: 'Space Grotesk Variable', 'Space Grotesk', sans-serif;
  }
</style>
