---
const matrixData = [
  {
    task: 'Redacción rápida de correos',
    chatgpt: 5,
    claude: 4,
    gemini: 3,
  },
  {
    task: 'Análisis profundo de contratos',
    chatgpt: 3,
    claude: 5,
    gemini: 4,
  },
  {
    task: 'Procesamiento de PDFs (OCR)',
    chatgpt: 2,
    claude: 3,
    gemini: 5,
  },
  {
    task: 'Integración Google Workspace',
    chatgpt: 1,
    claude: 1,
    gemini: 5,
  },
  {
    task: 'Casos multimodales (Video/Audio)',
    chatgpt: 4,
    claude: 2,
    gemini: 5,
  },
];
---

<section
  class="relative flex flex-col items-center justify-center py-20 bg-white dark:bg-[#0f172a] overflow-hidden"
>
  <div
    class="container max-w-6xl mx-auto px-4 md:px-10 flex flex-col items-center relative z-10"
  >
    <!-- Badge -->
    <div
      class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-[#f5f3ff] dark:bg-[#6324eb]/10 text-[#6324eb] text-xs font-black uppercase tracking-widest mb-8 animate-on-scroll font-display"
      data-animation="fade-in-up"
    >
      <span class="material-symbols-outlined text-[18px]">grid_view</span>
      Comparativa de Modelos
    </div>

    <!-- Title Group -->
    <div
      class="text-center max-w-3xl mx-auto mb-16 animate-on-scroll"
      data-animation="fade-in-up"
    >
      <h2
        class="text-[#111118] dark:text-white text-4xl md:text-6xl font-black leading-tight mb-6 font-display m-0"
      >
        Las Herramientas que Dominarás
      </h2>
      <p
        class="text-gray-500 dark:text-gray-400 text-lg md:text-xl font-medium leading-relaxed m-0"
      >
        Aprende a elegir la IA correcta según el tipo de tarea contable
      </p>
    </div>

    <!-- The Matrix Card -->
    <div
      class="w-full overflow-hidden rounded-3xl border border-gray-100 dark:border-white/5 shadow-[0_30px_60px_-12px_rgba(99,36,235,0.12)] animate-on-scroll"
      data-animation="scale-in"
      data-delay="200"
    >
      <div class="overflow-x-auto">
        <table class="w-full text-left border-collapse min-w-[750px]">
          <thead>
            <tr class="bg-gradient-to-r from-[#111022] to-[#6324eb] text-white">
              <th
                class="p-8 pl-10 text-xs font-black uppercase tracking-[0.2em] w-1/4 font-display opacity-90"
                >Tarea</th
              >
              <th class="p-8 text-center w-1/4">
                <div class="flex flex-col items-center gap-3">
                  <div
                    class="size-12 rounded-full bg-emerald-500/20 backdrop-blur-md border border-emerald-500/30 flex items-center justify-center text-emerald-400 shadow-[0_0_20px_rgba(16,185,129,0.3)]"
                  >
                    <span class="material-symbols-outlined text-2xl font-bold"
                      >bolt</span
                    >
                  </div>
                  <span
                    class="font-black text-[11px] uppercase tracking-widest font-display text-white/90"
                    >ChatGPT 4o</span
                  >
                </div>
              </th>
              <th class="p-8 text-center w-1/4">
                <div class="flex flex-col items-center gap-3">
                  <div
                    class="size-12 rounded-full bg-orange-500/20 backdrop-blur-md border border-orange-500/30 flex items-center justify-center text-orange-400 shadow-[0_0_20px_rgba(249,115,22,0.3)]"
                  >
                    <span class="material-symbols-outlined text-2xl font-bold"
                      >star</span
                    >
                  </div>
                  <span
                    class="font-black text-[11px] uppercase tracking-widest font-display text-white/90"
                    >Claude 3.5</span
                  >
                </div>
              </th>
              <th class="p-8 text-center w-1/4">
                <div class="flex flex-col items-center gap-3">
                  <div
                    class="size-12 rounded-full bg-blue-500/20 backdrop-blur-md border border-blue-500/30 flex items-center justify-center text-blue-400 shadow-[0_0_20px_rgba(59,130,246,0.3)]"
                  >
                    <span class="material-symbols-outlined text-2xl font-bold"
                      >hexagon</span
                    >
                  </div>
                  <span
                    class="font-black text-[11px] uppercase tracking-widest font-display text-white/90"
                    >Gemini 3</span
                  >
                </div>
              </th>
            </tr>
          </thead>
          <tbody
            class="bg-white dark:bg-white/5 divide-y divide-gray-50 dark:divide-white/5"
          >
            {
              matrixData.map(item => (
                <tr class="hover:bg-gray-50/50 dark:hover:bg-white/10 transition-colors group">
                  <td class="p-7 pl-10">
                    <span class="text-[#111118] dark:text-white font-bold text-lg font-display group-hover:text-[#6324eb] transition-colors">
                      {item.task}
                    </span>
                  </td>
                  <td class="p-7 text-center">
                    <div class="flex items-center justify-center gap-1.5">
                      {Array.from({ length: 5 }).map((_, i) => (
                        <span
                          class={`material-symbols-outlined text-lg ${i < item.chatgpt ? 'fill-active text-amber-400' : 'text-gray-200 dark:text-gray-700'}`}
                          style={
                            i < item.chatgpt
                              ? "font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24;"
                              : ''
                          }
                        >
                          star
                        </span>
                      ))}
                    </div>
                  </td>
                  <td class="p-7 text-center">
                    <div class="flex items-center justify-center gap-1.5">
                      {Array.from({ length: 5 }).map((_, i) => (
                        <span
                          class={`material-symbols-outlined text-lg ${i < item.claude ? 'fill-active text-amber-400' : 'text-gray-200 dark:text-gray-700'}`}
                          style={
                            i < item.claude
                              ? "font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24;"
                              : ''
                          }
                        >
                          star
                        </span>
                      ))}
                    </div>
                  </td>
                  <td class="p-7 text-center">
                    <div class="flex items-center justify-center gap-1.5">
                      {Array.from({ length: 5 }).map((_, i) => (
                        <span
                          class={`material-symbols-outlined text-lg ${i < item.gemini ? 'fill-active text-amber-400' : 'text-gray-200 dark:text-gray-700'}`}
                          style={
                            i < item.gemini
                              ? "font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24;"
                              : ''
                          }
                        >
                          star
                        </span>
                      ))}
                    </div>
                  </td>
                </tr>
              ))
            }
          </tbody>
        </table>
      </div>
    </div>

    <!-- Optional Detail Note -->
    <p
      class="mt-12 text-gray-400 dark:text-gray-500 text-sm font-medium animate-on-scroll"
      data-animation="fade-in-up"
      data-delay="400"
    >
      📋 Esto es solo una muestra. En el reto trabajarás con la matriz completa
      de 20+ casos de uso específicos para contabilidad mexicana, desde análisis
      de viáticos hasta respuestas a requerimientos SAT.
    </p>
  </div>
</section>

<style>
  .font-display {
    font-family: 'Space Grotesk Variable', 'Space Grotesk', sans-serif;
  }
</style>
