---
interface Props {
  ctaText?: string;
  className?: string;
}

const { ctaText = '🚀 Quiero asistir', className = '' } = Astro.props;
---

<section
  class={`w-full bg-white border-t border-slate-100 shadow-[0_-8px_30px_rgba(0,0,0,0.02)] z-10 relative font-display ${className}`}
>
  <div class="max-w-6xl mx-auto py-16 md:py-24 px-4 md:px-10">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
      <!-- Left Column: Headline & Subtext -->
      <div
        class="flex flex-col gap-4 text-center md:text-left animate-on-scroll"
        data-animation="fade-in-up"
      >
        <h2
          class="text-text-main text-3xl md:text-4xl lg:text-5xl font-extrabold leading-tight tracking-tight"
        >
          Accede al contenido <span class="text-primary italic">ahora</span>.
        </h2>
        <p
          class="text-text-muted text-lg font-medium leading-relaxed max-w-[480px] mx-auto md:mx-0"
        >
          Regístrate y recibe acceso inmediato a la masterclass grabada + 3
          casos prácticos completos con todos los prompts.
        </p>
      </div>

      <!-- Right Column: Button & Highlights -->
      <div class="flex flex-col items-center md:items-end gap-6">
        <!-- CTA Button -->
        <a
          href="#hero"
          class="group relative flex w-full md:w-auto min-w-[280px] cursor-pointer items-center justify-center overflow-hidden rounded-2xl h-16 px-10 bg-gradient-to-r from-primary to-indigo-600 text-white text-lg font-black tracking-wide transition-all duration-300 hover:-translate-y-1 hover:shadow-2xl hover:shadow-primary/40 active:scale-95 scroll-to-events btn-glow-premium"
        >
          <!-- Shine effect -->
          <div
            class="absolute inset-0 -translate-x-full group-hover:animate-shimmer-fast bg-gradient-to-r from-transparent via-white/20 to-transparent z-0 pointer-events-none"
          >
          </div>

          <span class="relative z-10 flex items-center gap-3">
            Ver Masterclass Ahora
          </span>
        </a>

        <!-- Features/Guarantees -->
        <div
          class="flex flex-wrap justify-center md:justify-end gap-3 md:gap-4 animate-on-scroll"
          data-animation="fade-in-up"
          data-delay="200"
        >
          <div
            class="flex items-center gap-2 px-4 py-2 rounded-xl bg-slate-50 border border-slate-100"
          >
            <span
              class="material-symbols-outlined text-primary text-[20px] font-bold"
              >videocam</span
            >
            <p
              class="text-text-main text-[10px] font-black uppercase tracking-[0.15em] m-0"
            >
              Acceso inmediato
            </p>
          </div>
          <div
            class="flex items-center gap-2 px-4 py-2 rounded-xl bg-slate-50 border border-slate-100"
          >
            <span
              class="material-symbols-outlined text-primary text-[20px] font-bold"
              >description</span
            >
            <p
              class="text-text-main text-[10px] font-black uppercase tracking-[0.15em] m-0"
            >
              PDF con 3 casos incluido
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes shimmer-fast {
    0% {
      transform: translateX(-100%);
    }
    100% {
      transform: translateX(100%);
    }
  }

  .group:hover .group-hover\:animate-shimmer-fast {
    animation: shimmer-fast 1.5s infinite;
  }

  @keyframes pulse-glow-premium {
    0%,
    100% {
      box-shadow: 0 0 15px rgba(55, 19, 236, 0.2);
    }
    50% {
      box-shadow: 0 0 35px rgba(55, 19, 236, 0.4);
    }
  }

  .btn-glow-premium {
    animation: pulse-glow-premium 3s infinite;
  }
</style>
