---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;

const learningItems = [
  {
    title: 'Mentalidad IA-First',
    description:
      'El framework de 3 preguntas para elegir qué IA usar según la tarea. No se trata de dominar una herramienta, sino de pensar estratégicamente.',
    icon: 'auto_awesome',
    delay: 100,
  },
  {
    title: 'Caso Práctico Paso a Paso',
    description:
      'Análisis de viáticos al extranjero: De 2 horas de trabajo manual a 12 minutos usando 4 IAs en secuencia (Perplexity → Claude → Gemini → ChatGPT).',
    icon: 'model_training',
    delay: 200,
  },
  {
    title: 'Matriz de Decisión',
    description:
      'Cuándo usar cada herramienta según el tipo de tarea: redacción, análisis profundo, búsqueda legal actualizada, o cálculos numéricos.',
    icon: 'bolt',
    delay: 300,
  },
  {
    title: 'Prompts Listos para Usar',
    description:
      'Plantillas estructuradas (Rol + Contexto + Tarea + Formato) que puedes copiar y adaptar para tus propios casos fiscales desde mañana.',
    icon: 'trending_up',
    delay: 400,
  },
];
---

<section
  class={`bg-background-light py-20 px-4 sm:px-6 relative overflow-hidden font-display ${className}`}
>
  <!-- Decorative background element -->
  <div
    class="absolute top-1/2 right-0 -translate-y-1/2 w-[400px] h-[400px] bg-primary/5 rounded-full blur-[100px] pointer-events-none -z-10"
  >
  </div>

  <div class="max-w-[800px] mx-auto flex flex-col gap-8 relative z-10">
    <!-- Section Header -->
    <div
      class="text-center md:text-left px-2 animate-on-scroll"
      data-animation="fade-in-up"
    >
      <h2
        class="text-text-main text-3xl md:text-4xl font-extrabold leading-tight tracking-tight"
      >
        📚 Lo que aprenderás en esta masterclass:
      </h2>
      <p class="mt-3 text-text-muted text-lg">
        Descubre cómo resolver casos fiscales reales en minutos usando flujos
        híbridos. Incluye 3 casos prácticos completos con todos los prompts.
      </p>
    </div>

    <!-- List Container -->
    <div class="flex flex-col gap-4">
      {
        learningItems.map(item => (
          <div
            class="group flex gap-5 bg-white p-5 rounded-2xl border border-slate-100 hover:border-primary/10 hover:shadow-card-ia transition-all duration-300 animate-on-scroll"
            data-animation="fade-in-up"
            data-delay={item.delay}
          >
            <div class="flex items-start justify-center pt-1 shrink-0">
              <div class="flex items-center justify-center rounded-xl bg-primary/10 text-primary size-12 group-hover:bg-primary group-hover:text-white transition-colors duration-300">
                <span class="material-symbols-outlined text-[24px]">
                  {item.icon}
                </span>
              </div>
            </div>
            <div class="flex flex-1 flex-col justify-center">
              <h3 class="text-text-main text-lg font-bold leading-snug mb-1 m-0">
                {item.title}
              </h3>
              <p class="text-text-muted text-base font-medium leading-relaxed m-0">
                {item.description}
              </p>
            </div>
          </div>
        ))
      }

      <!-- BONUS ITEM -->
      <div
        class="relative overflow-hidden flex gap-5 bg-amber-50 p-6 rounded-2xl border border-amber-100 shadow-sm mt-2 animate-on-scroll"
        data-animation="scale-in"
        data-delay="500"
      >
        <!-- Decorative subtle background pattern -->
        <div
          class="absolute -right-4 -top-4 opacity-10 rotate-12 pointer-events-none"
        >
          <span class="material-symbols-outlined text-[120px] text-amber-500"
            >card_giftcard</span
          >
        </div>
        <div
          class="flex items-start justify-center pt-1 shrink-0 relative z-10"
        >
          <div
            class="flex items-center justify-center rounded-xl bg-white shadow-sm text-amber-600 size-12"
          >
            <span class="material-symbols-outlined text-[24px]">redeem</span>
          </div>
        </div>
        <div class="flex flex-1 flex-col justify-center relative z-10">
          <div class="flex items-center gap-2 mb-1">
            <span
              class="inline-flex items-center px-2 py-0.5 rounded text-xs font-bold bg-amber-200 text-amber-800 uppercase tracking-wide"
            >
              Regalo Exclusivo
            </span>
          </div>
          <h3 class="text-amber-900 text-lg font-bold leading-snug mb-1 m-0">
            🎁 BONUS: 3 Casos Prácticos Completos
          </h3>
          <p
            class="text-amber-800/80 text-base font-medium leading-relaxed m-0"
          >
            Además del caso que vemos en video, recibirás el PDF con 2 casos
            adicionales: Revisión de CFDI masiva (500+ facturas) y Respuesta a
            requerimientos del SAT. Incluye todos los prompts y la matriz de
            decisión expandida.
          </p>
        </div>
      </div>
    </div>

    <!-- Bottom CTA -->
    <div
      class="flex justify-center pt-4 animate-on-scroll"
      data-animation="fade-in-up"
      data-delay="600"
    >
      <button
        class="flex items-center justify-center gap-2 bg-primary hover:bg-primary/90 text-white font-bold py-4 px-8 rounded-xl shadow-xl shadow-primary/30 transition-all hover:-translate-y-1 active:scale-95 w-full md:w-auto group"
      >
        <span>Ver Masterclass Ahora</span>
        <span
          class="material-symbols-outlined text-xl group-hover:translate-x-1 transition-transform"
          >arrow_forward</span
        >
      </button>
    </div>
  </div>
</section>
