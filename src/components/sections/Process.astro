---

---

<section class="section section-process" id="como-trabajo">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title fade-in-up">¿Cómo trabajo contigo?</h2>
      <p class="section-subtitle fade-in-up">
        Un proceso simple y directo donde te atiendo personalmente para resolver
        tus dudas fiscales específicas
      </p>
    </div>

    <div class="process-steps">
      <div class="process-step fade-in-up delay-100">
        <div class="step-indicator">
          <span class="step-number">1</span>
        </div>
        <h3 class="step-title">Agenda Directamente</h3>
        <p class="step-description">
          Selecciona el horario que mejor te convenga. Todas las citas las
          atiendo personalmente de lunes a viernes.
        </p>
      </div>

      <div class="process-step fade-in-up delay-200">
        <div class="step-indicator">
          <span class="step-number">2</span>
        </div>
        <h3 class="step-title">Videollamada Personal</h3>
        <p class="step-description">
          Nos conectamos por videollamada donde analizo tu situación específica
          y resuelvo todas tus dudas.
        </p>
      </div>

      <div class="process-step fade-in-up delay-300">
        <div class="step-indicator">
          <span class="step-number">3</span>
        </div>
        <h3 class="step-title">Soluciones Reales</h3>
        <p class="step-description">
          Te doy recomendaciones específicas: deducciones, estrategias fiscales,
          y optimización de impuestos.
        </p>
      </div>

      <div class="process-step fade-in-up delay-400">
        <div class="step-indicator">
          <span class="step-number">4</span>
        </div>
        <h3 class="step-title">Reporte Detallado</h3>
        <p class="step-description">
          Recibes un documento con todo lo revisado, recomendaciones y próximos
          pasos claros.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .section-process {
    background-color: var(--color-background-alt);
    position: relative;
  }

  .process-steps {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
    position: relative;
    padding-top: var(--space-8);
  }

  @media (min-width: 768px) {
    .process-steps {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .process-steps {
      grid-template-columns: repeat(4, 1fr);
      gap: var(--space-6);
    }
  }

  /* Connecting Line */
  @media (min-width: 1024px) {
    .process-steps::before {
      content: '';
      position: absolute;
      top: 60px;
      left: 10%;
      width: 80%;
      height: 2px;
      background: linear-gradient(
        90deg,
        var(--color-border-light) 0%,
        var(--color-primary) 50%,
        var(--color-border-light) 100%
      );
      z-index: 0;
      opacity: 0.5;
    }
  }

  .process-step {
    text-align: center;
    position: relative;
    z-index: 1;
    padding: var(--space-4);
    background: var(--color-white);
    border-radius: var(--border-radius-xl);
    border: 1px solid transparent;
    transition: all var(--transition-normal);
  }

  .process-step:hover {
    border-color: var(--color-primary-light);
    box-shadow: var(--shadow-lg);
    transform: translateY(-5px);
  }

  .step-indicator {
    width: 64px;
    height: 64px;
    background: var(--color-white);
    border: 2px solid var(--color-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-6);
    position: relative;
    box-shadow: 0 0 0 8px var(--color-background-alt); /* Creates gap around line */
    transition: all var(--transition-normal);
  }

  .process-step:hover .step-indicator {
    background: var(--color-primary);
    box-shadow:
      0 0 0 8px var(--color-background-alt),
      0 0 20px rgba(20, 184, 166, 0.4);
    transform: scale(1.1);
  }

  .step-number {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: 800;
    color: var(--color-primary);
    transition: color var(--transition-normal);
  }

  .process-step:hover .step-number {
    color: var(--color-white);
  }

  .step-title {
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text-primary);
  }

  .step-description {
    color: var(--color-text-secondary);
    font-size: var(--font-size-sm);
    line-height: 1.6;
  }
</style>
