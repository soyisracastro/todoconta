---

// Definimos las propuestas de valor
const valueProps = [
  {
    icon: "🛡️",
    title: "Garantía de Multas",
    description: "Si no cumplimos con alguna obligación fiscal por nuestra responsabilidad, cubrimos las multas y recargos correspondientes."
  },
  {
    icon: "⚡",
    title: "Servicio Integral",
    description: "Contabilidad, nómina, IMSS, SAT y más. Todo en un solo lugar con comunicación directa a tu contador asignado."
  },
  {
    icon: "🤖",
    title: "Automatización con IA",
    description: "Inteligencia artificial que automatiza procesos manuales, clasifica gastos y genera recomendaciones fiscales personalizadas."
  }
];
---

<section class="value-prop" id="beneficios">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title fade-in-up">¿Por qué nosotros?</h2>
      <p class="section-subtitle fade-in-up">
        Combinamos servicios contables profesionales con tecnología avanzada para darte tranquilidad total
      </p>
    </div>
    
    <div class="value-grid">
      {valueProps.map((prop, index) => (
        <div class="value-card fade-in-up" style={`animation-delay: ${index * 0.15}s`}>
          <div class="value-icon">{prop.icon}</div>
          <h3 class="value-title">{prop.title}</h3>
          <p class="value-description">{prop.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .value-prop {
    background: var(--color-background);
    padding: var(--space-20) 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-title {
    font-family: var(--font-secondary);
    font-size: var(--font-size-5xl);
    font-weight: 800;
    color: var(--color-text-primary);
    margin-bottom: var(--space-4);
  }

  .section-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
    margin-top: var(--space-12);
  }

  .value-card {
    text-align: center;
    padding: var(--space-8);
    background: var(--color-white);
    border-radius: var(--border-radius-xl);
    border: 1px solid var(--color-primary-light);
    transition: all var(--transition-slow);
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-primary);
  }

  .value-icon {
    width: 80px;
    height: 80px;
    background: var(--color-primary-light);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-4xl);
    margin: 0 auto var(--space-6);
    transition: all var(--transition-normal);
  }

  .value-card:hover .value-icon {
    background: var(--color-border-light);
    color: white;
    transform: scale(1.1);
  }

  .value-title {
    font-family: var(--font-secondary);
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: var(--space-4);
  }

  .value-description {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  /* Animaciones */
  .fade-in-up {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s ease;
  }

  .fade-in-up.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .value-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .value-grid {
      grid-template-columns: 1fr;
    }
    
    .section-title {
      font-size: var(--font-size-4xl);
    }
    
    .section-subtitle {
      font-size: var(--font-size-lg);
    }
  }

  @media (max-width: 480px) {
    .value-card {
      padding: var(--space-6);
    }
    
    .value-icon {
      width: 60px;
      height: 60px;
      font-size: var(--font-size-3xl);
    }
  }
</style>