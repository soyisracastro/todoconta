---

---

<section
  class="w-full relative overflow-hidden bg-primary py-16 lg:py-24"
  id="servicios-completos"
>
  <!-- Dot Pattern Background -->
  <div
    class="absolute inset-0 opacity-20 pointer-events-none"
    style="background-image: radial-gradient(#ffffff 1px, transparent 1px); background-size: 24px 24px;"
  >
  </div>

  <div class="container mx-auto px-6 lg:px-12 relative z-10 max-w-7xl">
    <div class="flex flex-col lg:flex-row items-center justify-between gap-10">
      <!-- Left Column: Content -->
      <div class="flex flex-col gap-6 text-left max-w-3xl animate-fade-in-up">
        <!-- Badge -->
        <div class="flex items-center gap-3">
          <span
            class="material-symbols-outlined text-white text-3xl font-variation-fill"
            >verified</span
          >
          <span class="text-white font-bold text-xs tracking-widest uppercase"
            >Planes Integrales</span
          >
        </div>

        <!-- Headline -->
        <h2
          class="text-4xl md:text-5xl lg:text-6xl font-[900] text-white tracking-tight leading-[1.1]"
        >
          ¿Necesitas servicios contables completos?
        </h2>

        <!-- Subheadline -->
        <p
          class="text-white/90 text-xl lg:text-2xl font-medium leading-relaxed"
        >
          Si además de asesoría puntual necesitas contabilidad mensual... <br
            class="hidden md:block"
          />
          ofrezco planes integrales desde <span
            class="font-black border-b-2 border-white/60">$3,500/mes</span
          >.
        </p>
      </div>

      <!-- Right Column: CTA -->
      <div
        class="flex-shrink-0 w-full lg:w-auto flex justify-start lg:justify-end animate-fade-in-up"
      >
        <a
          href="/servicios"
          class="group flex items-center justify-center gap-3 px-8 py-4 bg-transparent border-2 border-white rounded-xl text-white font-bold text-lg transition-all duration-300 hover:bg-white hover:text-primary shadow-lg hover:shadow-white/20 whitespace-nowrap"
        >
          <span>Ver Servicios Completos</span>
          <span
            class="material-symbols-outlined transition-transform group-hover:translate-x-1"
            >arrow_forward</span
          >
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  section {
    font-family: 'Manrope', sans-serif;
  }

  .font-variation-fill {
    font-variation-settings:
      'FILL' 1,
      'wght' 400,
      'GRAD' 0,
      'opsz' 24;
  }

  .animate-fade-in-up {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.8s ease-out forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
