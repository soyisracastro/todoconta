---
import Button from '../ui/Button.astro';
---

<section class="section services-cta" id="servicios-completos">
  <div class="container">
    <div class="cta-content fade-in-up">
      <div class="cta-text">
        <h3>¿Necesitas servicios contables completos?</h3>
        <p>
          Si además de asesoría puntual necesitas contabilidad mensual, nómina,
          o declaraciones, ofrezco planes integrales desde <strong
            >$3,500/mes</strong
          >.
        </p>
      </div>
      <div class="cta-action">
        <Button
          href="/servicios"
          variant="primary"
          size="lg"
          className="animate-pulse-glow"
        >
          Ver Servicios Completos
        </Button>
      </div>
    </div>
  </div>
</section>

<style>
  .services-cta {
    padding: var(--space-16) 0;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    position: relative;
    overflow: hidden;
  }

  /* Decorative background */
  .services-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(
      rgba(255, 255, 255, 0.1) 1px,
      transparent 1px
    );
    background-size: 20px 20px;
    opacity: 0.1;
  }

  .cta-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    padding: var(--space-10);
    border-radius: var(--border-radius-2xl);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: var(--shadow-xl);
    gap: var(--space-8);
  }

  .cta-text {
    flex: 1;
  }

  .cta-text h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-white);
    margin-bottom: var(--space-3);
  }

  .cta-text p {
    color: var(--color-hero-text-secondary);
    font-size: var(--font-size-lg);
    margin: 0;
    max-width: 600px;
  }

  .cta-text strong {
    color: var(--color-primary);
  }

  .cta-action {
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .cta-content {
      flex-direction: column;
      text-align: center;
      padding: var(--space-8);
    }

    .cta-action {
      width: 100%;
    }

    .cta-action :global(.btn) {
      width: 100%;
    }
  }
</style>
