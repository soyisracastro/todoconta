---

---
<section class="section services-link" id="servicios-completos">
    <div class="container">
        <div class="services-card fade-in-up">
            <h3>¿Necesitas servicios contables completos?</h3>
            <p>
                Si además de asesoría puntual necesitas contabilidad mensual, nómina, declaraciones o servicios integrales, 
                también ofrezco planes completos desde $4,200/mes con garantía de cumplimiento.
            </p>
            <a href="/servicios" class="btn btn-primary btn-lg">Ver Servicios Completos</a>
        </div>
    </div>
</section>

<style>
    /* Services Link */
    .services-link {
            background-color: var(--color-background-alt);
        }

        .services-card {
            background-color: var(--color-white);
            padding: var(--space-8);
            border: 1px solid var(--color-border-light);
            border-radius: var(--border-radius-xl);
            box-shadow: var(--shadow-md);
            max-width: 600px;
            margin: 0 auto;
            text-align: center;
        }

        .services-card h3 {
            font-family: var(--font-secondary);
            font-size: var(--font-size-2xl);
            font-weight: 600;
            margin-bottom: var(--space-4);
            color: var(--color-text-primary);
        }

        .services-card p {
            color: var(--color-text-secondary);
            margin-bottom: var(--space-6);
            line-height: 1.6;
        }
</style>