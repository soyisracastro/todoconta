---

---

<section
  class="relative overflow-hidden bg-white dark:bg-background-dark py-16 md:py-24"
  id="sobre-mi"
>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-20 items-center">
      <!-- Left Column: Content -->
      <div
        class="lg:col-span-7 flex flex-col justify-center order-2 lg:order-1 animate-fade-in-up"
      >
        <!-- Headline -->
        <div class="mb-8">
          <span
            class="inline-block py-1.5 px-4 rounded-full bg-primary/10 text-primary text-xs font-bold uppercase tracking-[0.15em] mb-6"
          >
            Sobre Mí
          </span>
          <h2
            class="text-[#111318] dark:text-white text-4xl md:text-5xl lg:text-6xl font-black leading-[1.05] tracking-tight"
          >
            Tu Contador de <span class="text-primary italic">Confianza.</span>
          </h2>
        </div>

        <!-- Body Text -->
        <div class="mb-10 max-w-2xl">
          <p
            class="text-slate-600 dark:text-gray-300 text-lg md:text-xl font-medium leading-relaxed"
          >
            Soy Israel Castro. He ayudado a más de 150 empresarios a simplificar
            sus impuestos y recuperar el control de sus finanzas. Mi objetivo es
            que entiendas tus números sin complicaciones.
          </p>
        </div>

        <!-- Quote Section -->
        <div class="mb-12 relative pl-8 border-l-4 border-primary/40">
          <span
            class="material-symbols-outlined absolute -top-4 -left-4 bg-white dark:bg-background-dark text-primary text-4xl p-1 rounded-full"
            >format_quote</span
          >
          <blockquote
            class="text-2xl md:text-3xl font-bold leading-tight text-[#111318] dark:text-white italic tracking-tight"
          >
            "La contabilidad debe ser tu aliada, no tu dolor de cabeza."
          </blockquote>
        </div>

        <!-- Stats Bar -->
        <div
          class="w-full border-t border-slate-100 dark:border-slate-800 pt-10"
        >
          <div class="grid grid-cols-3 gap-8 md:gap-16">
            <div class="flex flex-col gap-2">
              <p
                class="text-4xl md:text-5xl font-black text-primary tracking-tighter m-0"
              >
                15+
              </p>
              <p
                class="text-[11px] font-bold text-slate-400 dark:text-gray-500 uppercase tracking-[0.2em]"
              >
                Años Exp.
              </p>
            </div>
            <div
              class="flex flex-col gap-2 border-l border-slate-100 dark:border-slate-800 pl-8 md:pl-16"
            >
              <p
                class="text-4xl md:text-5xl font-black text-primary tracking-tighter m-0"
              >
                150+
              </p>
              <p
                class="text-[11px] font-bold text-slate-400 dark:text-gray-500 uppercase tracking-[0.2em]"
              >
                Clientes
              </p>
            </div>
            <div
              class="flex flex-col gap-2 border-l border-slate-100 dark:border-slate-800 pl-8 md:pl-16"
            >
              <p
                class="text-4xl md:text-5xl font-black text-primary tracking-tighter m-0"
              >
                100%
              </p>
              <p
                class="text-[11px] font-bold text-slate-400 dark:text-gray-500 uppercase tracking-[0.2em]"
              >
                Cumplimiento
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Right Column: Image -->
      <div
        class="lg:col-span-5 flex justify-center lg:justify-end order-1 lg:order-2 relative"
      >
        <!-- Abstract Decorative Elements -->
        <div
          class="absolute top-10 -right-10 w-72 h-72 bg-primary/10 rounded-full blur-[100px] -z-10"
        >
        </div>
        <div
          class="absolute -bottom-10 left-10 w-80 h-80 bg-blue-400/10 rounded-full blur-[100px] -z-10"
        >
        </div>

        <!-- Image Container -->
        <div
          class="relative w-full max-w-[440px] aspect-[4/5] rounded-[2.5rem] overflow-hidden shadow-[0_50px_100px_-20px_rgba(0,0,0,0.15)] bg-slate-100 dark:bg-gray-800 border-8 border-white dark:border-slate-800 group/image transition-transform duration-500 hover:scale-[1.02]"
        >
          <img
            alt="Israel Castro - Contador Especialista"
            class="w-full h-full object-cover object-top transition-transform duration-700 group-hover/image:scale-110"
            src="/images/photo.png"
          />

          <!-- Floating Badge -->
          <div
            class="absolute bottom-8 left-8 right-8 bg-white/90 dark:bg-gray-900/95 backdrop-blur-md p-5 rounded-2xl shadow-2xl border border-white/50 dark:border-gray-700 flex items-center gap-4 animate-float"
          >
            <div
              class="bg-primary shadow-[0_0_20px_rgba(19,55,236,0.3)] p-2.5 rounded-full text-white"
            >
              <span class="material-symbols-outlined text-2xl font-bold"
                >verified_user</span
              >
            </div>
            <div>
              <p
                class="text-[10px] text-slate-400 dark:text-gray-400 font-bold uppercase tracking-widest m-0"
              >
                Consultor Certificado
              </p>
              <p class="text-lg font-black text-[#111318] dark:text-white m-0">
                Israel Castro
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  section {
    font-family: 'Manrope', 'Inter', sans-serif;
  }

  .animate-fade-in-up {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }
</style>
