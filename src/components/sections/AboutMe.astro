---

---

<section class="about-me section" id="about-me">
  <div class="container">
    <div class="about-content">
      <div class="about-image-col fade-in-right">
        <div class="image-wrapper">
          <div class="image-frame">
            <img
              src="/images/photo.png"
              alt="Israel Castro Urieta - Contador Especialista"
              class="profile-image"
            />
          </div>
          <div class="experience-badge animate-float-delayed">
            <span class="years">15+</span>
            <span class="label">Años de<br />Experiencia</span>
          </div>
          <div class="decorative-blob"></div>
        </div>
      </div>

      <div class="about-text-col fade-in-left">
        <div class="section-header text-left">
          <span class="badge-sub">Sobre Mí</span>
          <h2 class="section-title">
            Tu Contador de <span class="text-gradient">Confianza</span>
          </h2>
        </div>

        <div class="about-description">
          <p class="lead-text">
            Ayudo a empresarios mexicanos a crecer sin preocuparse por temas
            fiscales.
          </p>

          <p>
            Soy <strong>Israel Castro</strong>, especialista en Contabilidad
            Fiscal con amplia experiencia en automatización de procesos para
            PyMEs. He ayudado a más de 150 empresarios a optimizar su carga
            fiscal y automatizar sus procesos.
          </p>

          <blockquote class="quote-card">
            "La contabilidad debe ser tu aliada, no tu dolor de cabeza.
            Desarrollo soluciones que simplifican lo complejo."
          </blockquote>

          <div class="stats-row">
            <div class="stat-item">
              <span class="stat-number">150+</span>
              <span class="stat-label">Clientes Felices</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">100%</span>
              <span class="stat-label">Cumplimiento</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-me {
    background: var(--color-white);
    position: relative;
    overflow: hidden;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
    align-items: center;
  }

  @media (min-width: 1024px) {
    .about-content {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-20);
    }

    /* Reorder on desktop to have text left, image right if preferred, 
       but current design has image left, text right which is fine */
  }

  /* Image Column */
  .about-image-col {
    position: relative;
    display: flex;
    justify-content: center;
  }

  .image-wrapper {
    position: relative;
    width: 100%;
    max-width: 400px;
  }

  .image-frame {
    position: relative;
    z-index: 2;
    border-radius: var(--border-radius-2xl);
    overflow: hidden;
    box-shadow: var(--shadow-2xl);
    border: 4px solid var(--color-white);
    aspect-ratio: 3/4;
    background: var(--color-background-alt);
  }

  .profile-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .image-frame:hover .profile-image {
    transform: scale(1.05);
  }

  .decorative-blob {
    position: absolute;
    top: -20px;
    left: -20px;
    width: 100%;
    height: 100%;
    background: var(--color-primary);
    border-radius: var(--border-radius-2xl);
    z-index: 1;
    opacity: 0.1;
    transform: rotate(-3deg);
    transition: transform var(--transition-normal);
  }

  .image-wrapper:hover .decorative-blob {
    transform: rotate(-6deg) scale(1.02);
  }

  .experience-badge {
    position: absolute;
    bottom: 30px;
    right: -20px;
    background: var(--color-white);
    padding: var(--space-4) var(--space-6);
    border-radius: var(--border-radius-xl);
    box-shadow: var(--shadow-xl);
    z-index: 3;
    display: flex;
    align-items: center;
    gap: var(--space-3);
    border: 1px solid var(--color-border-light);
  }

  .experience-badge .years {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: 800;
    color: var(--color-primary);
    line-height: 1;
  }

  .experience-badge .label {
    font-size: var(--font-size-xs);
    font-weight: 700;
    text-transform: uppercase;
    color: var(--color-text-secondary);
    line-height: 1.2;
  }

  /* Text Column */
  .badge-sub {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    background: rgba(20, 184, 166, 0.1);
    color: var(--color-primary);
    font-size: var(--font-size-sm);
    font-weight: 600;
    border-radius: var(--border-radius-full);
    margin-bottom: var(--space-4);
  }

  .section-header.text-left {
    text-align: left;
    margin-bottom: var(--space-6);
  }

  .section-header.text-left .section-title {
    text-align: left;
    margin-bottom: 0;
  }

  .lead-text {
    font-size: var(--font-size-lg);
    color: var(--color-text-primary);
    font-weight: 500;
    margin-bottom: var(--space-6);
  }

  .about-description p {
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
  }

  .quote-card {
    background: var(--color-background);
    padding: var(--space-6);
    border-left: 4px solid var(--color-primary);
    border-radius: 0 var(--border-radius-lg) var(--border-radius-lg) 0;
    font-style: italic;
    color: var(--color-text-primary);
    font-weight: 500;
    margin: var(--space-8) 0;
  }

  .stats-row {
    display: flex;
    gap: var(--space-12);
    margin-top: var(--space-8);
    padding-top: var(--space-8);
    border-top: 1px solid var(--color-border-light);
  }

  .stat-item {
    display: flex;
    flex-direction: column;
  }

  .stat-number {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: 800;
    color: var(--color-text-primary);
  }

  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    .experience-badge {
      right: 0;
      bottom: -20px;
    }

    .section-header.text-left {
      text-align: center;
    }

    .section-header.text-left .section-title {
      text-align: center;
    }

    .stats-row {
      justify-content: center;
    }
  }
</style>
