---

---

<section class="cta-section" id="contacto">
    <div class="cta-background"></div>
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title fade-in-up">¿Listo para la Tranquilidad Fiscal?</h2>
        <p class="cta-description fade-in-up">
          Agenda una consulta gratuita y descubre cómo podemos ayudarte a optimizar 
          la gestión fiscal de tu empresa. Sin compromisos.
        </p>
        
        <div class="cta-buttons fade-in-up">
          <a href="tel:5544753602" class="btn btn-primary btn-xl">
            <span class="btn-icon">📞</span>
            <span>(55) 4475-3602</span>
          </a>
          <a href="mailto:hola@todoconta.com" class="btn btn-secondary btn-xl">
            <span class="btn-icon">✉️</span>
            <span>hola@todoconta.com</span>
          </a>
        </div>
  
        <div class="contact-form fade-in-up">
          <h3 class="form-title">O déjanos tus datos y te contactamos</h3>
          <form id="contactForm" class="form">
            <div class="form-row">
              <div class="form-group">
                <label for="name" class="form-label">Nombre completo</label>
                <input type="text" id="name" name="name" class="form-input" placeholder="Tu nombre" required>
              </div>
              
              <div class="form-group">
                <label for="company" class="form-label">Empresa</label>
                <input type="text" id="company" name="company" class="form-input" placeholder="Nombre de tu empresa">
              </div>
            </div>
            
            <div class="form-row">
              <div class="form-group">
                <label for="email" class="form-label">Correo electrónico</label>
                <input type="email" id="email" name="email" class="form-input" placeholder="tu@email.com" required>
              </div>
              
              <div class="form-group">
                <label for="phone" class="form-label">Teléfono</label>
                <input type="tel" id="phone" name="phone" class="form-input" placeholder="(55) 1234-5678">
              </div>
            </div>
            
            <div class="form-group">
              <label for="message" class="form-label">¿Cómo podemos ayudarte?</label>
              <textarea id="message" name="message" class="form-input form-textarea" placeholder="Cuéntanos sobre tu situación fiscal o necesidades específicas" rows="4"></textarea>
            </div>
            
            <div class="form-group">
              <label class="form-checkbox">
                <input type="checkbox" name="privacy" required>
                <span class="checkbox-text">Acepto la <a href="/privacidad" class="link">política de privacidad</a> y recibir comunicaciones.</span>
              </label>
            </div>
            
            <button type="submit" class="btn btn-accent form-submit">
              <span class="btn-text">Enviar solicitud</span>
              <span class="btn-icon">→</span>
            </button>
          </form>
        </div>
  
        <div class="guarantee fade-in-up">
          <div class="guarantee-icon">🛡️</div>
          <div class="guarantee-text">
            Garantía de cumplimiento: Si no cumplimos por nuestra responsabilidad, cubrimos las multas
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <style>
    .cta-section {
      background: var(--bg-warm);
      padding: var(--space-24) 0;
      position: relative;
      overflow: hidden;
    }
  
    .cta-background {
      position: absolute;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM12 86c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm28-65c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm23-11c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-6 60c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm29 22c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zM32 63c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm57-13c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-9-21c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM60 91c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM35 41c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 60c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='%23f97316' fill-opacity='0.05' fill-rule='evenodd'/%3E%3C/svg%3E");
      opacity: 0.5;
    }
  
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-6);
      position: relative;
      z-index: 1;
    }
  
    .cta-content {
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }
  
    .cta-title {
      font-family: var(--font-display);
      font-size: var(--font-size-5xl);
      font-weight: 800;
      color: var(--neutral-900);
      margin-bottom: var(--space-6);
    }
  
    .cta-description {
      font-size: var(--font-size-lg);
      color: var(--neutral-600);
      margin-bottom: var(--space-8);
      line-height: 1.6;
    }
  
    .cta-buttons {
      display: flex;
      justify-content: center;
      gap: var(--space-6);
      flex-wrap: wrap;
      margin-bottom: var(--space-12);
    }
  
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-3) var(--space-6);
      font-family: var(--font-display);
      font-size: var(--font-size-base);
      font-weight: 600;
      border-radius: var(--border-radius-md);
      border: 2px solid transparent;
      cursor: pointer;
      text-decoration: none;
      transition: all var(--transition-normal);
      gap: var(--space-3);
    }
  
    .btn-xl {
      padding: var(--space-5) var(--space-10);
      font-size: var(--font-size-xl);
      border-radius: var(--border-radius-xl);
      font-weight: 700;
    }
  
    .btn-primary {
      background: var(--primary-600);
      color: white;
      border-color: var(--primary-600);
    }
  
    .btn-primary:hover {
      background: var(--primary-700);
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(2, 132, 199, 0.3);
    }
  
    .btn-secondary {
      background: var(--green-600);
      color: white;
      border-color: var(--green-600);
    }
  
    .btn-secondary:hover {
      background: var(--green-700);
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(22, 163, 74, 0.3);
    }
  
    .btn-accent {
      background: var(--orange-600);
      color: white;
      border-color: var(--orange-600);
      padding: var(--space-4) var(--space-8);
      font-size: var(--font-size-lg);
    }
  
    .btn-accent:hover {
      background: var(--orange-700);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(234, 88, 12, 0.3);
    }
  
    .btn-icon {
      font-size: 1.2em;
    }
  
    /* Formulario de contacto */
    .contact-form {
      background: var(--bg-primary);
      border-radius: var(--border-radius-2xl);
      padding: var(--space-8);
      box-shadow: var(--shadow-lg);
      border: 1px solid var(--neutral-200);
      margin-bottom: var(--space-12);
      text-align: left;
      transition: all var(--transition-normal);
    }
  
    .contact-form:hover {
      box-shadow: var(--shadow-xl);
      transform: translateY(-5px);
    }
  
    .form-title {
      font-family: var(--font-display);
      font-size: var(--font-size-xl);
      font-weight: 700;
      color: var(--neutral-900);
      margin-bottom: var(--space-6);
      text-align: center;
    }
  
    .form {
      display: flex;
      flex-direction: column;
      gap: var(--space-6);
    }
  
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-6);
    }
  
    .form-group {
      margin-bottom: var(--space-2);
    }
  
    .form-label {
      display: block;
      margin-bottom: var(--space-2);
      font-size: var(--font-size-sm);
      font-weight: 600;
      color: var(--neutral-800);
    }
  
    .form-input {
      width: 100%;
      padding: var(--space-3) var(--space-4);
      border: 2px solid var(--neutral-200);
      border-radius: var(--border-radius-lg);
      font-family: var(--font-primary);
      font-size: var(--font-size-base);
      color: var(--neutral-800);
      transition: all var(--transition-normal);
    }
  
    .form-input:focus {
      outline: none;
      border-color: var(--primary-500);
      box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.15);
    }
  
    .form-textarea {
      min-height: 120px;
      resize: vertical;
    }
  
    .form-checkbox {
      display: flex;
      align-items: flex-start;
      gap: var(--space-3);
      cursor: pointer;
    }
  
    .checkbox-text {
      font-size: var(--font-size-sm);
      color: var(--neutral-600);
      line-height: 1.5;
    }
  
    .link {
      color: var(--primary-600);
      text-decoration: underline;
      transition: color var(--transition-normal);
    }
  
    .link:hover {
      color: var(--primary-700);
    }
  
    .form-submit {
      margin-top: var(--space-2);
      align-self: flex-end;
    }
  
    /* Garantía */
    .guarantee {
      background: var(--green-50);
      border: 1px solid var(--green-200);
      border-radius: var(--border-radius-xl);
      padding: var(--space-6);
      margin-top: var(--space-8);
      display: flex;
      align-items: center;
      gap: var(--space-4);
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
      transition: all var(--transition-normal);
    }
  
    .guarantee:hover {
      transform: translateY(-3px);
      box-shadow: var(--shadow-md);
    }
  
    .guarantee-icon {
      width: 48px;
      height: 48px;
      background: var(--green-500);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: var(--font-size-2xl);
      color: white;
      flex-shrink: 0;
    }
  
    .guarantee-text {
      font-weight: 600;
      color: var(--green-800);
      font-size: var(--font-size-lg);
      line-height: 1.4;
    }
  
    /* Animaciones */
    .fade-in-up {
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.6s ease;
    }
  
    .fade-in-up.visible {
      opacity: 1;
      transform: translateY(0);
    }
  
    /* Responsive */
    @media (max-width: 768px) {
      .cta-title {
        font-size: var(--font-size-4xl);
      }
  
      .cta-description {
        font-size: var(--font-size-base);
      }
  
      .cta-buttons {
        flex-direction: column;
        gap: var(--space-4);
      }
  
      .btn-xl {
        width: 100%;
      }
  
      .form-row {
        grid-template-columns: 1fr;
        gap: var(--space-4);
      }
  
      .guarantee {
        flex-direction: column;
        text-align: center;
        padding: var(--space-4);
      }
    }
  
    @media (max-width: 480px) {
      .contact-form {
        padding: var(--space-4);
      }
  
      .form {
        gap: var(--space-4);
      }
    }
  </style>
  
  <script>
    // Script para manejar el envío del formulario
    document.addEventListener('DOMContentLoaded', () => {
      const contactForm = document.getElementById('contactForm');
      
      if (contactForm) {
        contactForm.addEventListener('submit', async (e) => {
          e.preventDefault();
          
          const submitButton = contactForm.querySelector('.form-submit');
          const originalButtonText = submitButton.innerHTML;
          
          // Cambiar texto del botón durante el envío
          submitButton.innerHTML = '<span class="btn-text">Enviando...</span>';
          submitButton.disabled = true;
          
          // Aquí iría el código para enviar el formulario (AJAX o fetch)
          // Por ejemplo:
          try {
            // Simular envío (reemplazar con tu lógica real)
            await new Promise(resolve => setTimeout(resolve, 1500));
            
            // Mostrar mensaje de éxito
            contactForm.innerHTML = `
              <div class="success-message">
                <div class="success-icon">✓</div>
                <h3 class="success-title">¡Solicitud enviada con éxito!</h3>
                <p class="success-text">Nos pondremos en contacto contigo a la brevedad posible.</p>
              </div>
            `;
          } catch (error) {
            // Restaurar botón y mostrar error
            submitButton.innerHTML = originalButtonText;
            submitButton.disabled = false;
            
            // Mostrar mensaje de error
            const errorDiv = document.createElement('div');
            errorDiv.className = 'form-error';
            errorDiv.textContent = 'Hubo un error al enviar el formulario. Por favor, inténtalo de nuevo.';
            
            contactForm.appendChild(errorDiv);
          }
        });
      }
      
      // Inicializar las animaciones
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
          }
        });
      }, {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      });
  
      document.querySelectorAll('.fade-in-up').forEach(el => observer.observe(el));
    });
  </script>