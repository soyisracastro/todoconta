---

---
<section class="section lead-magnet" id="guia-gratuita">
    <div class="container">
        <div class="lead-magnet-content">
            <div class="lead-magnet-text">
                <div class="lead-magnet-badge fade-in-up">
                    📊 GUÍA GRATUITA
                </div>
                <h2 class="lead-magnet-title fade-in-up">
                    "RESICO vs Régimen General: ¿Cuál te conviene más?"
                </h2>
                <p class="lead-magnet-subtitle fade-in-up">
                    Descarga mi guía completa donde analizo paso a paso cuándo es mejor mantenerte en RESICO 
                    y cuándo hacer la transición al régimen general según tu actividad específica.
                </p>
                
                <div class="lead-magnet-benefits fade-in-up">
                    <div class="benefit-item">
                        <span class="benefit-icon">✓</span>
                        <span>Comparativa detallada de ambos regímenes</span>
                    </div>
                    <div class="benefit-item">
                        <span class="benefit-icon">✓</span>
                        <span>Calculadora de límites RESICO 2025</span>
                    </div>
                    <div class="benefit-item">
                        <span class="benefit-icon">✓</span>
                        <span>Estrategias de optimización fiscal</span>
                    </div>
                    <div class="benefit-item">
                        <span class="benefit-icon">✓</span>
                        <span>Casos prácticos por profesión</span>
                    </div>
                </div>
            </div>
            
            <div class="lead-magnet-form fade-in-left">
                <div class="form-container">
                    <h3>Descarga GRATIS</h3>
                    <p>Solo necesito tu email para enviártela:</p>
                    
                    <form id="leadMagnetForm" class="download-form">
                        <div class="form-group">
                            <input 
                                type="text" 
                                class="form-field" 
                                placeholder="Tu nombre" 
                                required
                            >
                        </div>
                        <div class="form-group">
                            <input 
                                type="email" 
                                class="form-field" 
                                placeholder="tu@email.com" 
                                required
                            >
                        </div>
                        <div class="form-group">
                            <select class="form-field" required>
                                <option value="">¿Cuál es tu actividad?</option>
                                <option value="medico">Médico/Salud</option>
                                <option value="abogado">Abogado/Legal</option>
                                <option value="consultor">Consultor/Asesor</option>
                                <option value="arquitecto">Arquitecto/Ingeniero</option>
                                <option value="comercio">Comercio/Retail</option>
                                <option value="servicios">Servicios</option>
                                <option value="tecnologia">Tecnología</option>
                                <option value="otro">Otro</option>
                            </select>
                        </div>
                        
                        <button type="submit" class="btn btn-primary">
                            📥 Descargar Guía GRATIS
                        </button>
                        
                        <div class="form-disclaimer">
                            <input type="checkbox" required>
                            <span>Acepto recibir la guía y consejos fiscales por email</span>
                        </div>
                    </form>
                    
                    <div class="social-proof">
                        <span class="download-count">+1,247 profesionistas ya la descargaron</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* Lead Magnet Section */
.lead-magnet {
    background: linear-gradient(135deg, var(--color-background-alt) 0%, var(--color-white) 100%);
    border-top: 1px solid var(--color-border-light);
    border-bottom: 1px solid var(--color-border-light);
}

.lead-magnet-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
    align-items: center;
}

@media (min-width: 968px) {
    .lead-magnet-content {
        grid-template-columns: 1fr 1fr;
        gap: var(--space-20);
    }
}

.lead-magnet-text {
    text-align: center;
}

@media (min-width: 968px) {
    .lead-magnet-text {
        text-align: left;
    }
}

.lead-magnet-badge {
    display: inline-block;
    background-color: var(--color-primary);
    color: var(--color-white);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--border-radius-lg);
    font-size: var(--font-size-sm);
    font-weight: 600;
    margin-bottom: var(--space-4);
}

.lead-magnet-title {
    font-family: var(--font-secondary);
    font-size: var(--font-size-3xl);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: var(--space-4);
    color: var(--color-text-primary);
}

@media (min-width: 768px) {
    .lead-magnet-title {
        font-size: var(--font-size-4xl);
    }
}

.lead-magnet-subtitle {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-8);
    line-height: 1.6;
}

.lead-magnet-benefits {
    display: grid;
    gap: var(--space-4);
}

.benefit-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
}

.benefit-icon {
    color: var(--color-success);
    font-weight: bold;
    font-size: var(--font-size-lg);
    flex-shrink: 0;
}

.benefit-item span:last-child {
    color: var(--color-text-primary);
    font-weight: 500;
    font-size: var(--font-size-sm);
}

.lead-magnet-form {
    display: flex;
    justify-content: center;
    align-items: center;
}

.form-container {
    background-color: var(--color-white);
    padding: var(--space-8);
    border-radius: var(--border-radius-2xl);
    box-shadow: var(--shadow-lg);
    border: 1px solid var(--color-border-light);
    width: 100%;
    max-width: 420px;
    text-align: center;
}

.form-container h3 {
    font-family: var(--font-secondary);
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-2);
    color: var(--color-text-primary);
}

.form-container p {
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
    font-size: var(--font-size-base);
}

.download-form {
    display: grid;
    gap: var(--space-4);
}

.form-group {
    width: 100%;
}

.form-field {
    width: 100%;
    padding: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius-lg);
    font-size: var(--font-size-base);
    font-family: var(--font-primary);
    background-color: var(--color-background);
    color: var(--color-text-primary);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.form-field:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(61, 64, 91, 0.1);
}

.form-field::placeholder {
    color: var(--color-text-secondary);
}

.form-disclaimer {
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
    text-align: left;
}

.form-disclaimer input[type="checkbox"] {
    margin-top: 2px;
    flex-shrink: 0;
}

.form-disclaimer span {
    font-size: var(--font-size-xs);
    color: var(--color-text-secondary);
    line-height: 1.4;
}

.social-proof {
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border-light);
}

.download-count {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    font-style: italic;
}

/* Responsive adjustments */
@media (max-width: 767px) {
    .lead-magnet-title {
        font-size: var(--font-size-2xl);
    }
    
    .form-container {
        padding: var(--space-6);
    }
}
</style>