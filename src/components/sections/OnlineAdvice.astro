---
// Características detalladas de la asesoría online
const features = [
  {
    icon: "👨‍💼",
    title: "Contadores certificados",
    description: "Especialistas con experiencia comprobada en fiscalidad mexicana"
  },
  {
    icon: "⏰",
    title: "Disponible 9am-6pm",
    description: "Lunes a viernes, horario completo para tu conveniencia"
  },
  {
    icon: "📹",
    title: "Videollamada o chat",
    description: "Comunicación directa y personalizada según tus preferencias"
  },
  {
    icon: "📋",
    title: "Reporte por escrito",
    description: "Resumen detallado de la consulta para tus archivos"
  },
  {
    icon: "📱",
    title: "Sin instalaciones",
    description: "Acceso directo desde tu navegador, sin complicaciones"
  }
];
---

<section class="online-advice" id="asesoria">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title fade-in-up">Asesoría Fiscal 100% Online</h2>
      <p class="section-subtitle fade-in-up">
        Consultas especializadas por videollamada con contadores certificados. 
        Resuelve dudas fiscales específicas sin comprometerte a un plan mensual.
      </p>
    </div>
    
    <div class="content-grid">
      <div class="features-content">
        <div class="features-grid fade-in-up">
          {features.map((feature, index) => (
            <div class="feature-item" style={`animation-delay: ${index * 0.1}s`}>
              <div class="feature-icon">{feature.icon}</div>
              <div class="feature-content">
                <h4 class="feature-title">{feature.title}</h4>
                <p class="feature-description">{feature.description}</p>
              </div>
            </div>
          ))}
        </div>

        <div class="cta-buttons fade-in-up">
          <a href="#contacto" class="btn btn-primary btn-lg">Agendar Consulta</a>
          <a href="#servicios" class="btn btn-outline btn-lg">Ver Planes Mensuales</a>
        </div>
      </div>
      
      <div class="pricing-content fade-in-left">
        <div class="pricing-card">
          <div class="pricing-header">
            <div class="price-amount">$1,500</div>
            <div class="price-unit">por hora</div>
          </div>
          
          <div class="pricing-features">
            <div class="pricing-feature">✓ Consulta especializada</div>
            <div class="pricing-feature">✓ Reporte por escrito</div>
            <div class="pricing-feature">✓ Seguimiento incluido</div>
            <div class="pricing-feature">✓ Sin compromisos a largo plazo</div>
          </div>

          <a href="#contacto" class="pricing-cta">Agendar Ahora</a>

          <div class="availability">
            <div class="availability-header">
              <span class="availability-icon">⏰</span>
              <span>Próxima disponibilidad:</span>
            </div>
            <div class="availability-time">Hoy 2:00 PM - 4:00 PM</div>
          </div>
        </div>

        <div class="consultant-preview">
          <div class="consultant-card">
            <div class="consultant-avatar">CL</div>
            <div class="consultant-info">
              <div class="consultant-name">Carlos López, CPC</div>
              <div class="consultant-status">🟢 Disponible ahora</div>
            </div>
          </div>
          <div class="consultant-message">
            "¿Necesitas ayuda con tu situación fiscal específica?"
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .online-advice {
    background: var(--bg-primary);
    padding: var(--space-20) 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--neutral-900);
    margin-bottom: var(--space-4);
    line-height: 1.2;
  }

  .section-subtitle {
    font-size: var(--font-size-lg);
    color: var(--neutral-600);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .features-grid {
    display: grid;
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  .feature-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    padding: var(--space-5);
    background: var(--neutral-50);
    border-radius: var(--border-radius-lg);
    border-left: 4px solid var(--primary-600);
    transition: all var(--transition-normal);
  }

  .feature-item:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    background: var(--primary-50);
    border-left-color: var(--primary-500);
  }

  .feature-icon {
    font-size: var(--font-size-xl);
    min-width: 40px;
    text-align: center;
  }

  .feature-content {
    flex: 1;
  }

  .feature-title {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--neutral-900);
    margin-bottom: var(--space-1);
  }

  .feature-description {
    font-size: var(--font-size-sm);
    color: var(--neutral-600);
    line-height: 1.5;
    margin: 0;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-3) var(--space-6);
    font-family: var(--font-display);
    font-size: var(--font-size-base);
    font-weight: 600;
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    transition: all var(--transition-normal);
    border: 2px solid transparent;
  }

  .btn-lg {
    padding: var(--space-4) var(--space-8);
    font-size: var(--font-size-lg);
  }

  .btn-primary {
    background: var(--primary-600);
    color: white;
    border-color: var(--primary-600);
  }

  .btn-primary:hover {
    background: var(--primary-700);
    border-color: var(--primary-700);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
  }

  .btn-outline {
    background: transparent;
    color: var(--primary-600);
    border-color: var(--primary-600);
  }

  .btn-outline:hover {
    background: var(--primary-600);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
  }

  .pricing-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .pricing-card {
    background: var(--primary-600);
    color: white;
    padding: var(--space-8);
    border-radius: var(--border-radius-2xl);
    text-align: center;
    box-shadow: var(--shadow-xl);
    transition: all var(--transition-normal);
  }

  .pricing-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(37, 99, 235, 0.3);
  }

  .pricing-header {
    margin-bottom: var(--space-6);
  }

  .price-amount {
    font-family: var(--font-display);
    font-size: var(--font-size-5xl);
    font-weight: 800;
    line-height: 1;
    margin-bottom: var(--space-2);
  }

  .price-unit {
    font-size: var(--font-size-lg);
    opacity: 0.9;
  }

  .pricing-features {
    margin-bottom: var(--space-6);
    text-align: left;
  }

  .pricing-feature {
    padding: var(--space-2) 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--font-size-sm);
  }

  .pricing-feature:last-child {
    border-bottom: none;
  }

  .pricing-cta {
    display: block;
    background: white;
    color: var(--primary-600);
    padding: var(--space-4) var(--space-6);
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    font-weight: 600;
    margin-bottom: var(--space-6);
    transition: all var(--transition-normal);
  }

  .pricing-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(255, 255, 255, 0.3);
  }

  .availability {
    padding-top: var(--space-4);
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .availability-header {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: var(--space-2);
    font-size: var(--font-size-sm);
    opacity: 0.9;
  }

  .availability-time {
    font-weight: 600;
    font-size: var(--font-size-sm);
  }

  .consultant-preview {
    background: var(--green-50);
    padding: var(--space-6);
    border-radius: var(--border-radius-lg);
    border: 1px solid var(--green-200);
  }

  .consultant-card {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .consultant-avatar {
    width: 50px;
    height: 50px;
    background: var(--green-500);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-family: var(--font-display);
  }

  .consultant-info {
    flex: 1;
  }

  .consultant-name {
    font-weight: 600;
    color: var(--neutral-900);
    margin-bottom: var(--space-1);
  }

  .consultant-status {
    font-size: var(--font-size-sm);
    color: var(--green-700);
  }

  .consultant-message {
    font-size: var(--font-size-sm);
    color: var(--green-800);
    font-style: italic;
    line-height: 1.5;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .section-title {
      font-size: var(--font-size-3xl);
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
