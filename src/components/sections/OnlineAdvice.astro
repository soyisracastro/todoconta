---
export interface Props {
  cta?: {
    title: string;
    link: string;
  };
}

// Características detalladas de la asesoría online
const features = [
  {
    icon: "👨‍💼",
    title: "Atención personal directa conmigo",
    description: "Especialista con experiencia comprobada en fiscalidad mexicana"
  },
  {
    icon: "⏰",
    title: "Disponible 9am-6pm",
    description: "Lunes a viernes, horario completo para tu conveniencia"
  },
  {
    icon: "📹",
    title: "Videollamada o chat",
    description: "Comunicación directa y personalizada según tus preferencias"
  },
  {
    icon: "📋",
    title: "Reporte por escrito",
    description: "Resumen detallado de la consulta para tus archivos"
  },
];

const {
  cta,
} = Astro.props as Props;
---

<section class="online-advice" id="asesoria">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title fade-in-up">Asesoría Fiscal Personalizada<br /> para Profesionistas y Empresarios</h2>
      <p class="section-subtitle fade-in-up">
        Te ayudo personalmente a resolver tus dudas fiscales específicas por videollamada, sin comprometerte a un plan mensual.
      </p>
    </div>
    
    <div class="content-grid">
      <div class="features-content">
        <div class="features-grid fade-in-up">
          {features.map((feature, index) => (
            <div class="feature-item" style={`animation-delay: ${index * 0.1}s`}>
              <div class="feature-icon">{feature.icon}</div>
              <div class="feature-content">
                <h4 class="feature-title">{feature.title}</h4>
                <p class="feature-description">{feature.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
      
      <div class="pricing-content fade-in-left">
        <div class="pricing-card">
          <div class="pricing-header">
            <div class="price-amount">$1,200</div>
            <div class="price-unit">por hora</div>
          </div>
          
          <div class="pricing-features">
            <div class="pricing-feature">✓ Consulta especializada</div>
            <div class="pricing-feature">✓ Reporte por escrito</div>
            <div class="pricing-feature">✓ Seguimiento incluido</div>
            <div class="pricing-feature">✓ Sin compromisos a largo plazo</div>
          </div>

          {cta && <a href={cta.link} class="pricing-cta">{cta.title}</a>}

        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .online-advice {
    background: var(--color-background);
    padding: var(--space-8) 0;
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .features-grid {
    display: grid;
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-5);
    background: var(--color-white);
    border-radius: var(--border-radius-lg);
    border-left: 4px solid var(--color-primary);
    transition: all var(--transition-normal);
  }

  .feature-item:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    background: var(--color-background-alt);
    border-left-color: var(--color-border-light);
  }

  .feature-icon {
    font-size: var(--font-size-xl);
    min-width: 40px;
    text-align: center;
  }

  .feature-content {
    flex: 1;
  }

  .feature-title {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-text-primary);
    margin-top: var(--space-1);
    margin-bottom: var(--space-1);
  }

  .feature-description {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-3) var(--space-6);
    font-family: var(--font-secondary);
    font-size: var(--font-size-base);
    font-weight: 600;
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    transition: all var(--transition-normal);
    border: 2px solid transparent;
  }

  .btn-lg {
    padding: var(--space-4) var(--space-8);
    font-size: var(--font-size-lg);
  }

  .btn-primary {
    background: var(--color-primary);
    color: white;
    border-color: var(--color-primary);
  }

  .btn-primary:hover {
    background: var(--primary-700);
    border-color: var(--primary-700);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
  }

  .btn-outline {
    background: transparent;
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  .btn-outline:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
  }

  .pricing-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .pricing-card {
    background: var(--color-primary);
    color: white;
    padding: var(--space-8);
    border-radius: var(--border-radius-2xl);
    text-align: center;
    box-shadow: var(--shadow-xl);
    transition: all var(--transition-normal);
  }

  .pricing-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(37, 99, 235, 0.3);
  }

  .pricing-header {
    margin-bottom: var(--space-6);
  }

  .price-amount {
    font-family: var(--font-secondary);
    font-size: var(--font-size-5xl);
    font-weight: 800;
    line-height: 1;
    margin-bottom: var(--space-2);
  }

  .price-unit {
    font-size: var(--font-size-lg);
    opacity: 0.9;
  }

  .pricing-features {
    margin-bottom: var(--space-6);
    text-align: left;
  }

  .pricing-feature {
    padding: var(--space-2) 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--font-size-sm);
  }

  .pricing-feature:last-child {
    border-bottom: none;
  }

  .pricing-cta {
    display: block;
    background: white;
    color: var(--color-primary);
    padding: var(--space-4) var(--space-6);
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    font-weight: 600;
    margin-bottom: var(--space-6);
    transition: all var(--transition-normal);
  }

  .pricing-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(255, 255, 255, 0.3);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .section-title {
      font-size: var(--font-size-3xl);
    }

    .btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
