---

---

<section
  class="relative overflow-hidden bg-white dark:bg-background-dark py-16 lg:py-24"
  id="para-quien"
>
  <div class="relative mx-auto max-w-7xl px-6 lg:px-8">
    <!-- Section Header -->
    <div
      class="text-center space-y-4 max-w-3xl mx-auto mb-16 animate-fade-in-up"
    >
      <h2
        class="text-[#111814] dark:text-white text-3xl md:text-4xl lg:text-5xl font-black tracking-tight leading-tight"
      >
        ¿Este servicio es para ti?
      </h2>
      <p
        class="text-slate-600 dark:text-gray-300 text-lg md:text-xl font-medium leading-relaxed"
      >
        Mi asesoría está especializada en personas físicas con actividades
        empresariales y profesionales.
      </p>
    </div>

    <!-- Cards Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12">
      <!-- Card 1: Profesionistas -->
      <div
        class="group relative flex flex-col gap-8 rounded-3xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900/50 p-10 shadow-sm hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 animate-fade-in-up"
      >
        <div class="flex items-center gap-6">
          <div
            class="flex items-center justify-center size-16 rounded-2xl bg-primary/10 text-primary group-hover:bg-primary group-hover:text-white transition-all duration-500 transform group-hover:rotate-6"
          >
            <span class="material-symbols-outlined text-[36px]"
              >business_center</span
            >
          </div>
          <h3 class="text-3xl font-bold text-[#111814] dark:text-white">
            Profesionistas
          </h3>
        </div>

        <div class="h-px w-full bg-slate-100 dark:bg-slate-800"></div>

        <ul class="flex flex-col gap-5">
          {
            [
              'Médicos y Personal de Salud',
              'Abogados y Notarios',
              'Consultores de Negocios',
              'Arquitectos e Ingenieros',
              'Dentistas y Especialistas',
            ].map(item => (
              <li class="flex items-start gap-4 group/item">
                <span class="material-symbols-outlined text-primary mt-1 shrink-0 font-bold transition-transform group-hover/item:scale-110">
                  check_circle
                </span>
                <span class="text-slate-600 dark:text-gray-200 text-lg font-medium">
                  {item}
                </span>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Card 2: Empresarios -->
      <div
        class="group relative flex flex-col gap-8 rounded-2xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900/50 p-8 shadow-sm hover:shadow-xl hover:-translate-y-2 transition-all duration-300 animate-fade-in-up"
        style="animation-delay: 0.2s;"
      >
        <div class="flex items-center gap-6">
          <div
            class="flex items-center justify-center size-16 rounded-2xl bg-primary/10 text-primary group-hover:bg-primary group-hover:text-white transition-all duration-500 transform group-hover:rotate-6"
          >
            <span class="material-symbols-outlined text-[36px]">storefront</span
            >
          </div>
          <h3 class="text-3xl font-bold text-[#111814] dark:text-white">
            Empresarios
          </h3>
        </div>

        <div class="h-px w-full bg-slate-100 dark:bg-slate-800"></div>

        <ul class="flex flex-col gap-5">
          {
            [
              'Comerciantes (Retail y Mayoristas)',
              'Empresas de Servicios',
              'Manufactura y Producción',
              'Sector Tecnológico y Software',
              'Agencias de Marketing y Diseño',
            ].map(item => (
              <li class="flex items-start gap-4 group/item">
                <span class="material-symbols-outlined text-primary mt-1 shrink-0 font-bold transition-transform group-hover/item:scale-110">
                  check_circle
                </span>
                <span class="text-slate-600 dark:text-gray-200 text-lg font-medium">
                  {item}
                </span>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <!-- Call to Action -->
    <div
      class="flex justify-center pt-12 animate-fade-in-up"
      style="animation-delay: 0.4s;"
    >
      <p class="text-sm text-center text-slate-500 dark:text-gray-400 max-w-lg">
        ¿No ves tu actividad aquí? <a
          class="text-primary font-bold hover:text-primary-dark transition-colors no-underline border-b-2 border-primary/20 hover:border-primary"
          href="#contacto">Contáctame</a
        > para evaluar tu caso específico.
      </p>
    </div>
  </div>
</section>

<style>
  /* Animations logic is handled by global scripts but we can add local tweaks */
  .animate-fade-in-up {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.8s ease-out forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
