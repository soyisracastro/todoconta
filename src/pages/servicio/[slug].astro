---
// src/pages/servicio/[slug].astro
import { getCollection } from 'astro:content';

// Genera rutas para cada servicio
export async function getStaticPaths() {
  const services = await getCollection('services');
  return services.map(service => ({
    params: { slug: service.slug },
    props: { service }
  }));
}

const { service } = Astro.props;
const { 
  title, 
  description, 
  monthlyPrice, 
  yearlyPrice,
  savings,
  priceNote,
  badge,
  featured,
  recommendedFor,
  heroImage,
  ctaText,
  ctaUrl,
  secondaryCtaText,
  secondaryCtaUrl,
  includedFeatures = [],
  categories = [],
  testimonials = [],
  faqs = [],
  process = [],
  stats = []
} = service.data;

// Formatear el precio
const formatPrice = (price) => {
  return new Intl.NumberFormat('es-MX', {
    style: 'currency',
    currency: 'MXN',
    maximumFractionDigits: 0
  }).format(price || 0);
};

const formattedMonthlyPrice = formatPrice(monthlyPrice);
const formattedYearlyPrice = yearlyPrice ? formatPrice(yearlyPrice) : null;

// Generar URL de placeholder para imágenes
const getPlaceholderImage = (width: number, height: number, text: string2120000) => {
  return `https://placehold.co/${width}x${height}/075985/FFFFFF/png?text=${encodeURIComponent(text || 'Todoconta')}`;
};

const heroPlaceholder = getPlaceholderImage(800, 450, title);
const testimonialPlaceholder = getPlaceholderImage(80, 80, 'TC');
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title} | Todoconta</title>
  <meta name="description" content={description}>
  
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    :root {
      /* Primary Brand Colors */
      --primary-50: #f0f9ff;
      --primary-100: #e0f2fe;
      --primary-200: #bae6fd;
      --primary-300: #7dd3fc;
      --primary-400: #38bdf8;
      --primary-500: #0ea5e9;
      --primary-600: #0284c7;
      --primary-700: #0369a1;
      --primary-800: #075985;
      --primary-900: #0c4a6e;

      /* Green Financial */
      --green-50: #f0fdf4;
      --green-100: #dcfce7;
      --green-200: #bbf7d0;
      --green-300: #86efac;
      --green-400: #4ade80;
      --green-500: #22c55e;
      --green-600: #16a34a;
      --green-700: #15803d;
      --green-800: #166534;
      --green-900: #14532d;

      /* Orange Energetic */
      --orange-50: #fff7ed;
      --orange-100: #ffedd5;
      --orange-200: #fed7aa;
      --orange-300: #fdba74;
      --orange-400: #fb923c;
      --orange-500: #f97316;
      --orange-600: #ea580c;
      --orange-700: #c2410c;
      --orange-800: #9a3412;
      --orange-900: #7c2d12;

      /* Warm Neutrals */
      --neutral-50: #fafaf9;
      --neutral-100: #f5f5f4;
      --neutral-200: #e7e5e4;
      --neutral-300: #d6d3d1;
      --neutral-400: #a8a29e;
      --neutral-500: #78716c;
      --neutral-600: #57534e;
      --neutral-700: #44403c;
      --neutral-800: #292524;
      --neutral-900: #1c1917;
      
      --font-primary: 'Inter', sans-serif;
      --font-display: 'Plus Jakarta Sans', sans-serif;
      
      --space-1: 0.25rem;
      --space-2: 0.5rem;
      --space-3: 0.75rem;
      --space-4: 1rem;
      --space-5: 1.25rem;
      --space-6: 1.5rem;
      --space-8: 2rem;
      --space-10: 2.5rem;
      --space-12: 3rem;
      --space-16: 4rem;
      --space-20: 5rem;
      
      --border-radius-sm: 0.25rem;
      --border-radius-md: 0.375rem;
      --border-radius-lg: 0.5rem;
      --border-radius-xl: 0.75rem;
      --border-radius-2xl: 1rem;
      --border-radius-3xl: 1.5rem;
      
      --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: var(--font-primary);
      line-height: 1.6;
      color: var(--neutral-700);
      background-color: white;
    }
    
    img {
      max-width: 100%;
      height: auto;
      display: block;
    }
    
    a {
      color: var(--primary-600);
      text-decoration: none;
      transition: color 0.2s ease;
    }
    
    a:hover {
      color: var(--primary-700);
    }
    
    h1, h2, h3, h4, h5, h6 {
      font-family: var(--font-display);
      color: var(--neutral-900);
      line-height: 1.2;
    }
    
    h1 {
      font-size: 2.5rem;
      font-weight: 800;
      margin-bottom: var(--space-6);
    }
    
    h2 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: var(--space-5);
    }
    
    h3 {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: var(--space-4);
    }
    
    p {
      margin-bottom: var(--space-4);
    }
    
    ul {
      list-style-position: inside;
      margin-bottom: var(--space-4);
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-4);
    }
    
    .header {
      background-color: white;
      padding: var(--space-4) 0;
      border-bottom: 1px solid var(--neutral-200);
      position: sticky;
      top: 0;
      z-index: 100;
    }
    
    .header .container {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .logo {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1.5rem;
      color: var(--neutral-900);
    }
    
    .nav ul {
      display: flex;
      list-style: none;
      gap: var(--space-6);
      margin: 0;
    }
    
    .nav a {
      font-weight: 500;
      color: var(--neutral-700);
    }
    
    .nav a:hover {
      color: var(--primary-600);
    }
    
    .breadcrumbs {
      background-color: var(--neutral-100);
      padding: var(--space-3) 0;
      border-bottom: 1px solid var(--neutral-200);
    }
    
    .breadcrumbs ul {
      display: flex;
      list-style: none;
      gap: var(--space-2);
      margin: 0;
    }
    
    .breadcrumbs li:not(:last-child)::after {
      content: '/';
      margin-left: var(--space-2);
      color: var(--neutral-400);
    }
    
    .breadcrumbs li:last-child {
      color: var(--neutral-700);
      font-weight: 500;
    }
    
    .section {
      padding: var(--space-16) 0;
    }
    
    .section-title {
      text-align: center;
      margin-bottom: var(--space-10);
    }
    
    .section-subtitle {
      text-align: center;
      max-width: 700px;
      margin: 0 auto var(--space-10);
      color: var(--neutral-600);
      font-size: 1.125rem;
    }
    
    .text-center {
      text-align: center;
    }
    
    /* Hero Section */
    .hero {
      padding: var(--space-16) 0;
      background-color: var(--neutral-50);
      position: relative;
      overflow: hidden;
    }
    
    .hero-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
      align-items: center;
    }
    
    .hero-badge {
      display: inline-block;
      background-color: var(--primary-600);
      color: white;
      font-size: 0.75rem;
      font-weight: 600;
      padding: var(--space-1) var(--space-3);
      border-radius: var(--border-radius-full);
      margin-bottom: var(--space-4);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .hero-description {
      color: var(--neutral-600);
      margin-bottom: var(--space-6);
      font-size: 1.125rem;
    }
    
    .hero-recommended {
      background-color: white;
      padding: var(--space-4);
      border-radius: var(--border-radius-lg);
      margin-bottom: var(--space-6);
      border: 1px solid var(--neutral-200);
    }
    
    .hero-recommended strong {
      display: block;
      margin-bottom: var(--space-1);
      color: var(--neutral-800);
    }
    
    .hero-actions {
      display: flex;
      gap: var(--space-4);
      margin-bottom: var(--space-8);
    }
    
    .btn {
      display: inline-block;
      padding: var(--space-3) var(--space-6);
      border-radius: var(--border-radius-lg);
      font-weight: 600;
      font-family: var(--font-display);
      transition: all 0.2s;
      text-align: center;
    }
    
    .btn-primary {
      background-color: var(--primary-600);
      color: white;
    }
    
    .btn-primary:hover {
      background-color: var(--primary-700);
      color: white;
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }
    
    .btn-outline {
      border: 2px solid var(--primary-600);
      color: var(--primary-600);
      background-color: transparent;
    }
    
    .btn-outline:hover {
      background-color: var(--primary-50);
      transform: translateY(-2px);
    }
    
    .hero-image {
      border-radius: var(--border-radius-2xl);
      overflow: hidden;
      box-shadow: var(--shadow-lg);
    }
    
    .hero-image img {
      width: 100%;
      height: auto;
    }
    
    /* Pricing Section */
    .pricing {
      background-color: var(--neutral-50);
      padding: var(--space-16) 0;
      position: relative;
      overflow: hidden;
    }
    
    .pricing::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23000000' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      opacity: 0.5;
    }
    
    .pricing-options {
      background-color: white;
      border-radius: var(--border-radius-lg);
      padding: var(--space-1);
      display: flex;
      max-width: 300px;
      margin: 0 auto var(--space-10);
      border: 1px solid var(--neutral-200);
    }
    
    .pricing-option {
      flex: 1;
      padding: var(--space-2) var(--space-4);
      text-align: center;
      cursor: pointer;
      border-radius: var(--border-radius-md);
      font-weight: 500;
      transition: background-color 0.2s;
    }
    
    .pricing-option.active {
      background-color: var(--primary-600);
      color: white;
    }
    
    .pricing-cards {
      position: relative;
      z-index: 1;
    }
    
    .pricing-card {
      background-color: white;
      border-radius: var(--border-radius-2xl);
      padding: var(--space-8);
      max-width: 500px;
      margin: 0 auto;
      box-shadow: var(--shadow-lg);
      position: relative;
      overflow: hidden;
    }
    
    .pricing-card.featured {
      border: 2px solid var(--primary-500);
    }
    
    .pricing-card.featured::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--primary-500), var(--primary-700));
    }
    
    .pricing-title {
      text-align: center;
      margin-bottom: var(--space-6);
    }
    
    .pricing-amount {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 800;
      color: var(--primary-700);
      text-align: center;
      margin-bottom: var(--space-2);
    }
    
    .pricing-period {
      text-align: center;
      color: var(--neutral-500);
      margin-bottom: var(--space-6);
    }
    
    .feature-list {
      list-style: none;
      margin-bottom: var(--space-8);
    }
    
    .feature-item {
      display: flex;
      align-items: flex-start;
      gap: var(--space-3);
      margin-bottom: var(--space-3);
      padding-bottom: var(--space-3);
      border-bottom: 1px dashed var(--neutral-200);
    }
    
    .feature-item:last-child {
      border-bottom: none;
      margin-bottom: 0;
      padding-bottom: 0;
    }
    
    .feature-icon {
      color: var(--green-500);
      flex-shrink: 0;
      font-size: 1.25rem;
    }
    
    .cta-button {
      display: block;
      background-color: var(--primary-600);
      color: white;
      text-align: center;
      padding: var(--space-4);
      border-radius: var(--border-radius-lg);
      font-weight: 600;
      font-size: 1.125rem;
      font-family: var(--font-display);
      transition: all 0.2s;
    }
    
    .cta-button:hover {
      background-color: var(--primary-700);
      color: white;
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }
    
    /* Categories Section */
    .categories {
      padding: var(--space-16) 0;
    }
    
    .categories-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: var(--space-6);
    }
    
    .category-card {
      background-color: white;
      border-radius: var(--border-radius-xl);
      padding: var(--space-6);
      box-shadow: var(--shadow-md);
      border: 1px solid var(--neutral-200);
      height: 100%;
    }
    
    .category-title {
      font-family: var(--font-display);
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--neutral-900);
      padding-bottom: var(--space-3);
      margin-bottom: var(--space-4);
      border-bottom: 1px solid var(--neutral-200);
    }
    
    .category-features {
      list-style: none;
    }
    
    .category-feature {
      display: flex;
      align-items: center;
      padding: var(--space-2) 0;
      border-bottom: 1px dashed var(--neutral-200);
    }
    
    .category-feature:last-child {
      border-bottom: none;
    }
    
    .feature-status {
      width: 20px;
      height: 20px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: var(--space-3);
      flex-shrink: 0;
      font-size: 0.75rem;
      font-weight: bold;
    }
    
    .status-included {
      background-color: var(--green-100);
      color: var(--green-600);
    }
    
    .status-excluded {
      background-color: var(--neutral-200);
      color: var(--neutral-500);
    }
    
    .feature-info {
      display: flex;
      align-items: center;
      position: relative;
    }
    
    .feature-tooltip {
      margin-left: var(--space-2);
      width: 16px;
      height: 16px;
      border-radius: 50%;
      background-color: var(--neutral-200);
      color: var(--neutral-600);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      font-weight: bold;
      cursor: help;
      position: relative;
    }
    
    .feature-tooltip::before {
      content: attr(data-tooltip);
      position: absolute;
      bottom: 100%;
      left: 50%;
      transform: translateX(-50%);
      background-color: var(--neutral-800);
      color: white;
      text-align: center;
      padding: var(--space-2) var(--space-3);
      border-radius: var(--border-radius-md);
      font-size: 0.75rem;
      white-space: nowrap;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.2s, visibility 0.2s;
      z-index: 10;
      margin-bottom: var(--space-2);
      pointer-events: none;
    }
    
    .feature-tooltip:hover::before {
      opacity: 1;
      visibility: visible;
    }
    
    /* Testimonials Section */
    .testimonials {
      background-color: var(--primary-50);
      padding: var(--space-16) 0;
    }
    
    .testimonials-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: var(--space-6);
    }
    
    .testimonial-card {
      background-color: white;
      border-radius: var(--border-radius-xl);
      padding: var(--space-6);
      box-shadow: var(--shadow-md);
      position: relative;
      height: 100%;
      display: flex;
      flex-direction: column;
    }
    
    .quote-icon {
      position: absolute;
      top: var(--space-4);
      left: var(--space-4);
      font-size: 3rem;
      line-height: 1;
      color: var(--primary-200);
      font-family: serif;
      font-weight: bold;
      opacity: 0.5;
    }
    
    .testimonial-quote {
      padding-top: var(--space-6);
      margin-bottom: var(--space-6);
      font-style: italic;
      color: var(--neutral-700);
      flex-grow: 1;
    }
    
    .testimonial-author {
      display: flex;
      align-items: center;
      border-top: 1px solid var(--neutral-200);
      padding-top: var(--space-4);
      margin-top: auto;
    }
    
    .author-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      overflow: hidden;
      margin-right: var(--space-3);
      flex-shrink: 0;
    }
    
    .author-name {
      font-weight: 600;
      color: var(--neutral-900);
      margin-bottom: var(--space-1);
      font-size: 1rem;
    }
    
    .author-position {
      color: var(--neutral-500);
      font-size: 0.875rem;
    }
    
    /* Process Section */
    .process {
      padding: var(--space-16) 0;
    }
    
    .process-steps {
      position: relative;
      max-width: 800px;
      margin: 0 auto;
    }
    
    .process-steps::before {
      content: '';
      position: absolute;
      left: 24px;
      top: 0;
      bottom: 0;
      width: 2px;
      background-color: var(--primary-100);
      z-index: 0;
    }
    
    .process-step {
      position: relative;
      padding-left: 64px;
      margin-bottom: var(--space-10);
    }
    
    .process-step:last-child {
      margin-bottom: 0;
    }
    
    .step-number {
      position: absolute;
      left: 0;
      top: 0;
      width: 48px;
      height: 48px;
      background-color: var(--primary-600);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      font-size: 1.25rem;
      z-index: 1;
      box-shadow: var(--shadow-md);
    }
    
    .step-content {
      background-color: white;
      border-radius: var(--border-radius-xl);
      padding: var(--space-6);
      box-shadow: var(--shadow-md);
      border: 1px solid var(--neutral-200);
    }
    
    .step-icon {
      font-size: 2rem;
      margin-bottom: var(--space-3);
    }
    
    .step-title {
      font-size: 1.25rem;
      margin-bottom: var(--space-3);
    }
    
    /* Stats Section */
    .stats {
      background-color: var(--primary-600);
      color: white;
      padding: var(--space-12) 0;
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--space-8);
      text-align: center;
    }
    
    .stat-item {
      padding: var(--space-4);
    }
    
    .stat-value {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: var(--space-2);
      line-height: 1;
    }
    
    .stat-label {
      font-size: 1.125rem;
      font-weight: 500;
      margin-bottom: var(--space-2);
      opacity: 0.9;
    }
    
    .stat-description {
      font-size: 0.875rem;
      opacity: 0.7;
    }
    
    /* FAQ Section */
    .faq {
      padding: var(--space-16) 0;
    }

    .faq-items {
      max-width: 800px;
      margin: 0 auto;
    }

    .faq-item {
      border-bottom: 1px solid var(--neutral-200);
      margin-bottom: var(--space-4);
    }

    .faq-question {
      font-family: var(--font-display);
      font-size: 1.125rem;
      font-weight: 600;
      color: var(--neutral-900);
      padding: var(--space-4) 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      cursor: pointer;
      transition: color 0.2s;
      width: 100%;
      background: transparent;
      border: none;
      text-align: left;
    }

    .faq-question:hover {
      color: var(--primary-600);
    }

    .question-icon {
      font-size: 1.5rem;
      color: var(--primary-600);
      transition: transform 0.3s;
      margin-left: var(--space-2);
      min-width: 24px;
      text-align: center;
    }

    .faq-question[aria-expanded="true"] .question-icon {
      transform: rotate(45deg);
    }

    .faq-answer {
      padding: 0;
      color: var(--neutral-600);
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.5s ease-out, padding 0.3s ease-out;
    }

    .faq-question[aria-expanded="true"] + .faq-answer {
      padding-bottom: var(--space-4);
    }
    
    /* CTA Section */
    .cta {
      background: linear-gradient(135deg, var(--primary-600) 0%, var(--primary-800) 100%);
      color: white;
      padding: var(--space-16) 0;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .cta::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url("data:image/svg+xml,%3Csvg width='52' height='26' viewBox='0 0 52 26' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Cpath d='M10 10c0-2.21-1.79-4-4-4-3.314 0-6-2.686-6-6h2c0 2.21 1.79 4 4 4 3.314 0 6 2.686 6 6 0 2.21 1.79 4 4 4 3.314 0 6 2.686 6 6 0 2.21 1.79 4 4 4v2c-3.314 0-6-2.686-6-6 0-2.21-1.79-4-4-4-3.314 0-6-2.686-6-6zm25.464-1.95l8.486 8.486-1.414 1.414-8.486-8.486 1.414-1.414z' /%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      opacity: 0.3;
    }
    
    .cta-content {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto;
    }
    
    .cta-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: var(--space-4);
    }
    
    .cta-description {
      font-size: 1.125rem;
      margin-bottom: var(--space-8);
      opacity: 0.9;
    }
    
    .cta-actions {
      display: flex;
      gap: var(--space-4);
      justify-content: center;
    }
    
    /* Footer */
    .footer {
      background-color: var(--neutral-900);
      color: white;
      padding: var(--space-16) 0 var(--space-8);
    }
    
    .footer-content {
      display: grid;
      grid-template-columns: 1.5fr repeat(3, 1fr);
      gap: var(--space-8);
      margin-bottom: var(--space-8);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      padding-bottom: var(--space-8);
    }
    
    .footer-logo {
      color: white;
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1.5rem;
      margin-bottom: var(--space-4);
    }
    
    .footer-desc {
      color: var(--neutral-400);
      font-size: 0.875rem;
      margin-bottom: var(--space-6);
    }
    
    .footer-social {
      display: flex;
      gap: var(--space-4);
    }
    
    .social-link {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background-color: rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      transition: background-color 0.2s;
    }
    
    .social-link:hover {
      background-color: var(--primary-600);
      color: white;
    }
    
    .footer-column h3 {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: var(--space-4);
      color: white;
    }
    
    .footer-column ul {
      list-style: none;
      margin: 0;
    }
    
    .footer-column li {
      margin-bottom: var(--space-2);
    }
    
    .footer-column a {
      color: var(--neutral-400);
      transition: color 0.2s;
    }
    
    .footer-column a:hover {
      color: white;
    }
    
    .footer-bottom {
      text-align: center;
      color: var(--neutral-500);
      font-size: 0.875rem;
    }
    
    /* Responsive Styles */
    @media (max-width: 992px) {
      .hero-content {
        grid-template-columns: 1fr;
      }
      
      .hero-image {
        order: -1;
        max-width: 500px;
        margin: 0 auto;
      }
      
      .footer-content {
        grid-template-columns: 1fr 1fr;
      }
      
      .footer-logo-col {
        grid-column: 1 / -1;
        text-align: center;
        margin-bottom: var(--space-6);
      }
      
      .footer-social {
        justify-content: center;
      }
    }
    
    @media (max-width: 768px) {
      .header .container {
        flex-direction: column;
        gap: var(--space-4);
      }
      
      .hero-actions {
        flex-direction: column;
      }
      
      .btn {
        width: 100%;
      }
      
      .categories-grid {
        grid-template-columns: 1fr;
      }
      
      .testimonials-grid {
        grid-template-columns: 1fr;
      }
      
      .cta-actions {
        flex-direction: column;
        align-items: center;
      }
      
      .cta-actions .btn {
        width: 100%;
        max-width: 300px;
      }
      
      .footer-content {
        grid-template-columns: 1fr;
        text-align: center;
      }
      
      .footer-column ul {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: var(--space-4);
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="container">
      <a href="/" class="logo">Todoconta</a>
      <nav class="nav">
        <ul>
          <li><a href="/servicios">Servicios</a></li>
          <li><a href="/tienda">Productos</a></li>
          <li><a href="/info">Recursos</a></li>
          <li><a href="/contacto">Contacto</a></li>
        </ul>
      </nav>
    </div>
  </header>
  
  <!-- Breadcrumbs -->
  <div class="breadcrumbs">
    <div class="container">
      <ul>
        <li><a href="/">Inicio</a></li>
        <li><a href="/servicios">Servicios</a></li>
        <li>{title}</li>
      </ul>
    </div>
  </div>
  
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-text">
          {badge && <div class="hero-badge">{badge}</div>}
          <h1 class="hero-title">{title}</h1>
          <p class="hero-description">{description}</p>
          
          {recommendedFor && (
            <div class="hero-recommended">
              <strong>Recomendado para:</strong> {recommendedFor}
            </div>
          )}
          
          <div class="hero-actions">
            <a href={ctaUrl || "#pricing"} class="btn btn-primary">
              {ctaText || "Contratar Ahora"}
            </a>
            
            {secondaryCtaText && secondaryCtaUrl && (
              <a href={secondaryCtaUrl} class="btn btn-outline">
                {secondaryCtaText}
              </a>
            )}
          </div>
        </div>
        
        <div class="hero-image">
          <img src={heroImage || heroPlaceholder} alt={title}>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Pricing Section -->
  <section class="pricing" id="pricing">
    <div class="container">
      <h2 class="section-title">Inversión</h2>
      
      {yearlyPrice && (
        <div class="pricing-options">
          <div class="pricing-option active" data-plan="monthly">Mensual</div>
          <div class="pricing-option" data-plan="yearly">
            Anual {savings && <span>({savings})</span>}
          </div>
        </div>
      )}
      
      <div class="pricing-cards">
        <div class="pricing-card" id="pricing-card" class:list={{"featured": featured}}>
          <div id="monthly-plan" style="display: block;">
            <div class="pricing-amount">{formattedMonthlyPrice}</div>
            <div class="pricing-period">{priceNote || "mensual"}</div>
          </div>
          
          {yearlyPrice && (
            <div id="yearly-plan" style="display: none;">
              <div class="pricing-amount">{formattedYearlyPrice}</div>
              <div class="pricing-period">anual ({savings})</div>
            </div>
          )}
          
          {includedFeatures && includedFeatures.length > 0 && (
            <ul class="feature-list">
              {includedFeatures.filter(f => f.included).map(feature => (
                <li class="feature-item">
                  <span class="feature-icon">{feature.icon}</span>
                  <span>{feature.text}</span>
                </li>
              ))}
            </ul>
          )}
          
          <a href={ctaUrl || "#"} class="cta-button">
            {ctaText || "Contratar Ahora"}
          </a>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Categories Section -->
  {categories && categories.length > 0 && (
    <section class="categories section">
      <div class="container">
        <h2 class="section-title">¿Qué Incluye Nuestro Servicio?</h2>
        <p class="section-subtitle">
          Conoce a detalle todo lo que obtendrás al contratar nuestro servicio
        </p>
        
        <div class="categories-grid">
          {categories.map(category => (
            <div class="category-card">
              <h3 class="category-title">{category.title}</h3>
              
              <ul class="category-features">
                {category.features.map(feature => (
                  <li class="category-feature">
                    <span class={`feature-status ${feature.included ? 'status-included' : 'status-excluded'}`}>
                      {feature.included ? '✓' : '✕'}
                    </span>
                    
                    <div class="feature-info">
                      <span class={feature.included ? '' : 'text-muted'}>
                        {feature.text}
                      </span>
                      
                      {feature.tooltip && (
                        <span class="feature-tooltip" data-tooltip={feature.tooltip}>i</span>
                      )}
                    </div>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
  
  <!-- Process Section -->
  {process && process.length > 0 && (
    <section class="process section">
      <div class="container">
        <h2 class="section-title">Cómo Funciona Nuestro Proceso</h2>
        <p class="section-subtitle">
          Conoce el paso a paso de nuestra metodología de trabajo
        </p>
        
        <div class="process-steps">
          {process.map((step, index) => (
            <div class="process-step">
              <div class="step-number">{index + 1}</div>
              <div class="step-content">
                <div class="step-icon">{step.icon}</div>
                <h3 class="step-title">{step.title}</h3>
                <p class="step-description">{step.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
  
  <!-- Testimonials Section -->
  {testimonials && testimonials.length > 0 && (
    <section class="testimonials section">
      <div class="container">
        <h2 class="section-title">Lo Que Dicen Nuestros Clientes</h2>
        <p class="section-subtitle">
          Conoce las experiencias de quienes ya confían en nuestros servicios
        </p>
        
        <div class="testimonials-grid">
          {testimonials.map(testimonial => (
            <div class="testimonial-card">
              <div class="quote-icon">"</div>
              <p class="testimonial-quote">{testimonial.quote}</p>
              
              <div class="testimonial-author">
                <div class="author-avatar">
                  <img 
                    src={testimonial.avatar || testimonialPlaceholder} 
                    alt={testimonial.author}
                  >
                </div>
                
                <div class="author-info">
                  <h4 class="author-name">{testimonial.author}</h4>
                  <p class="author-position">
                    {testimonial.position}
                    {testimonial.position && testimonial.company && ", "}
                    {testimonial.company}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
  
  <!-- Stats Section -->
  {stats && stats.length > 0 && (
    <section class="stats">
      <div class="container">
        <div class="stats-grid">
          {stats.map(stat => (
            <div class="stat-item">
              <div class="stat-value">{stat.value}</div>
              <div class="stat-label">{stat.label}</div>
              {stat.description && <p class="stat-description">{stat.description}</p>}
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
  
  <!-- FAQ Section -->
  {faqs && faqs.length > 0 && (
    <section class="faq section">
      <div class="container">
        <h2 class="section-title">Preguntas Frecuentes</h2>
        <p class="section-subtitle">
          Resolvemos tus dudas más comunes sobre nuestro servicio
        </p>
        
        <div class="faq-items">
          {faqs.map((faq, index) => (
            <div class="faq-item">
              <button 
                class="faq-question" 
                aria-expanded={index === 0 ? "true" : "false"} 
                type="button"
              >
                <span>{faq.question}</span>
                <span class="question-icon">{index === 0 ? '−' : '+'}</span>
              </button>
              <div 
                class="faq-answer" 
                style={index === 0 ? "max-height: 1000px; padding-bottom: var(--space-4);" : "max-height: 0;"}
              >
                <p>{faq.answer}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}
  
  <!-- CTA Section -->
  <section class="cta">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">¿Listo para comenzar con nuestro {title}?</h2>
        <p class="cta-description">
          Contrata ahora y obtén el mejor servicio contable para tu negocio.
        </p>
        
        <div class="cta-actions">
          <a href={ctaUrl || "#pricing"} class="btn btn-primary">
            {ctaText || "Contratar Ahora"}
          </a>
          
          {secondaryCtaText && secondaryCtaUrl && (
            <a href={secondaryCtaUrl} class="btn btn-outline">
              {secondaryCtaText}
            </a>
          )}
        </div>
      </div>
    </div>
  </section>
  
  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-logo-col">
          <div class="footer-logo">Todoconta</div>
          <p class="footer-desc">Servicios contables y fiscales profesionales</p>
          
          <div class="footer-social">
            <a href="#" class="social-link">FB</a>
            <a href="#" class="social-link">TW</a>
            <a href="#" class="social-link">IG</a>
            <a href="#" class="social-link">LI</a>
          </div>
        </div>
        
        <div class="footer-column">
          <h3>Servicios</h3>
          <ul>
            <li><a href="/servicio/plan-basico">Plan Básico</a></li>
            <li><a href="/servicio/plan-premium">Plan Premium</a></li>
            <li><a href="/servicio/plan-empresarial">Plan Empresarial</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h3>Productos</h3>
          <ul>
            <li><a href="/landing/xmlsat-premium">XMLSAT Premium</a></li>
            <li><a href="/landing/control-xml-cfdi">Control XML-CFDI</a></li>
            <li><a href="/tienda">Ver todos</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h3>Información</h3>
          <ul>
            <li><a href="/info">Centro de Recursos</a></li>
            <li><a href="/contacto">Contacto</a></li>
            <li><a href="/privacidad">Aviso de Privacidad</a></li>
          </ul>
        </div>
      </div>
      
      <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} Todoconta. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
  
  <script>
    // Funcionalidad para cambiar entre precios mensuales y anuales
    document.addEventListener('DOMContentLoaded', () => {
      const pricingOptions = document.querySelectorAll('.pricing-option');
      const monthlyPlan = document.getElementById('monthly-plan');
      const yearlyPlan = document.getElementById('yearly-plan');
      
      if (pricingOptions.length > 0 && monthlyPlan && yearlyPlan) {
        pricingOptions.forEach(option => {
          option.addEventListener('click', () => {
            // Remover clase active de todas las opciones
            pricingOptions.forEach(opt => opt.classList.remove('active'));
            
            // Añadir clase active a la opción seleccionada
            option.classList.add('active');
            
            // Mostrar plan correspondiente
            const plan = option.getAttribute('data-plan');
            if (plan === 'monthly') {
              monthlyPlan.style.display = 'block';
              yearlyPlan.style.display = 'none';
            } else {
              monthlyPlan.style.display = 'none';
              yearlyPlan.style.display = 'block';
            }
          });
        });
      }
      
      const faqQuestions = document.querySelectorAll('.faq-question');

      faqQuestions.forEach(question => {
        question.addEventListener('click', () => {
          const isExpanded = question.getAttribute('aria-expanded') === 'true';
          const answer = question.nextElementSibling;
          
          // Cerrar todas las respuestas primero
          faqQuestions.forEach(q => {
            if (q !== question) {
              q.setAttribute('aria-expanded', 'false');
              q.nextElementSibling.style.maxHeight = '0';
              // Actualizar el icono
              const icon = q.querySelector('.question-icon');
              if (icon) icon.textContent = '+';
            }
          });
          
          // Cambiar estado de la pregunta actual
          question.setAttribute('aria-expanded', !isExpanded);
          
          // Actualizar altura máxima e icono
          if (!isExpanded) {
            // El valor 1000px es seguro para contener la mayoría de los textos
            // pero podrías calcularlo de forma dinámica si es necesario
            answer.style.maxHeight = '1000px';
            const icon = question.querySelector('.question-icon');
            if (icon) icon.textContent = '−';
          } else {
            answer.style.maxHeight = '0';
            const icon = question.querySelector('.question-icon');
            if (icon) icon.textContent = '+';
          }
        });
      });
    });
  </script>
</body>
</html>