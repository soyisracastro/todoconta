---
import LandingLayout from '@/layouts/LandingLayout.astro';
import Button from '@/components/ui/Button.astro';

const title = '¡Gracias por Descargar! | TodoConta';
const description =
  'Revisa tu email para acceder a los casos prácticos y recursos gratuitos de IA para contadores.';
const keywords = ['descarga confirmada', 'recursos IA contadores'];

// Configuration
const RETO_DETAILS_URL = '/ia-contadores/reto-12-dias';
const PDF_DOWNLOAD_LINK =
  'https://s3.us-east-1.amazonaws.com/todoconta.com/uploads/Recursos-IA-Contadores.pdf'; // TODO: Update with actual PDF URL
---

<LandingLayout
  title={title}
  description={description}
  keywords={keywords}
  showFooter={false}
>
  <!-- THANK YOU HERO -->
  <section class="thank-you-hero">
    <div class="hero-gradient-bg"></div>
    <div class="container">
      <div
        class="success-icon animate-on-scroll"
        data-animation="scale-in"
        data-delay="100"
      >
        ✅
      </div>

      <h1
        class="title animate-on-scroll"
        data-animation="fade-in-up"
        data-delay="200"
      >
        ¡Perfecto! <span class="gradient">Ya Casi Está</span>
      </h1>

      <p
        class="subtitle animate-on-scroll"
        data-animation="fade-in-up"
        data-delay="300"
      >
        Hemos enviado un email a tu bandeja de entrada con el enlace de descarga
        a tus recursos gratuitos.
      </p>

      <div
        class="checklist animate-on-scroll"
        data-animation="fade-in-up"
        data-delay="400"
      >
        <div class="checklist-item">
          <div class="check-icon">📧</div>
          <div class="check-content">
            <h3 class="check-title">Paso 1: Revisa tu Email</h3>
            <p class="check-description">
              En menos de 5 minutos recibirás un email con el PDF descargable
            </p>
          </div>
        </div>

        <div class="checklist-item">
          <div class="check-icon">📂</div>
          <div class="check-content">
            <h3 class="check-title">Paso 2: Revisa tu carpeta de Spam</h3>
            <p class="check-description">
              Si no lo ves en tu bandeja principal, revisa promociones o spam
            </p>
          </div>
        </div>

        <div class="checklist-item">
          <div class="check-icon">📥</div>
          <div class="check-content">
            <h3 class="check-title">Paso 3: Descarga y Aplica</h3>
            <p class="check-description">
              Abre el PDF y empieza a usar los casos prácticos inmediatamente
            </p>
          </div>
        </div>
      </div>

      <!-- Direct Download Button (Backup) -->
      <div
        class="backup-download animate-on-scroll"
        data-animation="fade-in"
        data-delay="500"
      >
        <p class="backup-text">¿No llegó el email?</p>
        <Button
          href={PDF_DOWNLOAD_LINK}
          variant="secondary"
          size="lg"
          class="btn-backup"
          data-track-event="direct_download_backup"
        >
          📥 Descargar Directamente Aquí
        </Button>
      </div>
    </div>
  </section>

  <!-- QUÉ INCLUYE EL PDF -->
  <section class="whats-included">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">📦 Lo Que Vas a Encontrar</h2>
      </div>

      <div class="included-grid">
        <div
          class="included-card animate-on-scroll"
          data-animation="slide-in-left"
          data-delay="100"
        >
          <div class="card-number">01</div>
          <h3 class="card-title">2 Casos Prácticos Completos</h3>
          <p class="card-description">
            Flujos paso a paso con capturas de pantalla y todos los prompts
            necesarios
          </p>
        </div>

        <div
          class="included-card animate-on-scroll"
          data-animation="fade-in-up"
          data-delay="200"
        >
          <div class="card-number">02</div>
          <h3 class="card-title">10 Plantillas de Prompts</h3>
          <p class="card-description">
            Estructura ROL + CONTEXTO + TAREA + FORMATO lista para copiar/pegar
          </p>
        </div>

        <div
          class="included-card animate-on-scroll"
          data-animation="slide-in-right"
          data-delay="300"
        >
          <div class="card-number">03</div>
          <h3 class="card-title">Matriz de Decisión</h3>
          <p class="card-description">
            Tabla de referencia rápida: cuándo usar ChatGPT, Claude o Gemini
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SIGUIENTE PASO: RETO -->
  <section class="next-step">
    <div class="container">
      <div class="next-step-content animate-on-scroll" data-animation="fade-in-up">
        <div class="step-badge">🎯 SIGUIENTE NIVEL</div>

        <h2 class="step-title">
          ¿Listo para Dominar IA en tu Práctica Contable?
        </h2>

        <p class="step-description">
          Estos recursos son solo el inicio. Si quieres ir más profundo y
          dominar ChatGPT, Claude, Gemini 3, NotebookLM y Perplexity con casos
          prácticos reales, únete al <strong>Reto de 12 Días</strong>.
        </p>

        <div class="benefits-list">
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <span>12 sesiones en vivo (5-30 enero 2026)</span>
          </div>
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <span>Casos prácticos avanzados paso a paso</span>
          </div>
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <span>Comunidad privada + soporte directo</span>
          </div>
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <span>Flujos híbridos que ahorran 10-15h semanales</span>
          </div>
        </div>

        <div class="step-ctas">
          <Button
            href={RETO_DETAILS_URL}
            variant="primary"
            size="xl"
            class="btn-step btn-gold"
            data-track-event="cta_reto_from_thank_you"
          >
            🚀 Ver Detalles del Reto
          </Button>
        </div>

        <p class="step-note">Plazas limitadas | Comienza el 5 de enero 2026</p>
      </div>
    </div>
  </section>

  <!-- SOCIAL SHARE (Optional) -->
  <section class="social-share">
    <div class="container">
      <div class="share-content animate-on-scroll" data-animation="fade-in-up">
        <h3 class="share-title">¿Te pareció útil este recurso?</h3>
        <p class="share-description">
          Compártelo con otros contadores que quieran aprovechar IA
        </p>

        <div class="share-buttons">
          <a
            href="https://twitter.com/intent/tweet?text=Descarga%20gratis%203%20casos%20pr%C3%A1cticos%20de%20IA%20para%20contadores%20%F0%9F%93%84%20&url=https://todoconta.com/ia-contadores/recursos-gratis"
            target="_blank"
            rel="noopener noreferrer"
            class="share-button twitter"
            data-track-event="share_twitter"
          >
            <span class="share-icon">𝕏</span>
            <span>Compartir en Twitter</span>
          </a>

          <a
            href="https://www.linkedin.com/sharing/share-offsite/?url=https://todoconta.com/ia-contadores/recursos-gratis"
            target="_blank"
            rel="noopener noreferrer"
            class="share-button linkedin"
            data-track-event="share_linkedin"
          >
            <span class="share-icon">in</span>
            <span>Compartir en LinkedIn</span>
          </a>

          <a
            href="https://www.facebook.com/sharer/sharer.php?u=https://todoconta.com/ia-contadores/recursos-gratis"
            target="_blank"
            rel="noopener noreferrer"
            class="share-button facebook"
            data-track-event="share_facebook"
          >
            <span class="share-icon">f</span>
            <span>Compartir en Facebook</span>
          </a>
        </div>
      </div>
    </div>
  </section>
</LandingLayout>

<style>
  /* ========================================
     VARIABLES Y GLOBALS
     ======================================== */
  :root {
    --bg-dark: #0f0518;
    --bg-darker: #000000;
    --bg-card: rgba(255, 255, 255, 0.05);
    --text-main: #ffffff;
    --text-muted: #9ca3af;
    --accent-purple: #a855f7;
    --accent-gold: #eab308;
    --accent-teal: #14b8a6;
    --color-success: #10b981;
  }

  section {
    padding: var(--space-16) 0;
    color: var(--text-main);
    position: relative;
    overflow: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-4);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .section-title {
    font-size: var(--font-size-3xl);
    font-weight: 800;
    line-height: 1.2;
    color: #ffffff !important;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
    opacity: 1 !important;
    background: none !important;
    -webkit-background-clip: unset !important;
    -webkit-text-fill-color: #ffffff !important;
    background-clip: unset !important;
  }

  /* ========================================
     ANIMATIONS
     ======================================== */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes scaleIn {
    from {
      opacity: 0;
      transform: scale(0.5);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes gradientShift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .animate-on-scroll {
    opacity: 0;
  }

  .animate-on-scroll.animated {
    animation-duration: 0.8s;
    animation-fill-mode: forwards;
    animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animated[data-animation='fade-in-up'] {
    animation-name: fadeInUp;
  }
  .animated[data-animation='fade-in'] {
    animation-name: fadeIn;
  }
  .animated[data-animation='slide-in-left'] {
    animation-name: slideInLeft;
  }
  .animated[data-animation='slide-in-right'] {
    animation-name: slideInRight;
  }
  .animated[data-animation='scale-in'] {
    animation-name: scaleIn;
  }

  /* ========================================
     THANK YOU HERO
     ======================================== */
  .thank-you-hero {
    background: linear-gradient(to bottom, #000000, #1e1b4b);
    padding: var(--space-24) 0;
    text-align: center;
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .hero-gradient-bg {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 50% 30%,
      rgba(16, 185, 129, 0.2),
      transparent 60%
    );
    pointer-events: none;
  }

  .success-icon {
    font-size: 120px;
    margin-bottom: var(--space-6);
    line-height: 1;
  }

  .title {
    font-size: var(--font-size-5xl);
    font-weight: 800;
    margin-bottom: var(--space-6);
    line-height: 1.1;
    color: #ffffff;
    position: relative;
    z-index: 1;
  }

  .gradient {
    background: linear-gradient(to right, #10b981, #14b8a6, #06b6d4);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    display: inline-block;
  }

  .subtitle {
    font-size: var(--font-size-xl);
    color: #e2e8f0;
    max-width: 700px;
    margin: 0 auto var(--space-12);
    line-height: 1.6;
    position: relative;
    z-index: 1;
  }

  .checklist {
    max-width: 700px;
    margin: 0 auto var(--space-12);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    position: relative;
    z-index: 1;
  }

  .checklist-item {
    display: flex;
    align-items: start;
    gap: var(--space-4);
    background: var(--bg-card);
    padding: var(--space-6);
    border-radius: var(--border-radius-xl);
    border: 1px solid rgba(16, 185, 129, 0.2);
    text-align: left;
    transition: all 0.3s ease;
  }

  .checklist-item:hover {
    border-color: var(--color-success);
    box-shadow: 0 10px 30px rgba(16, 185, 129, 0.2);
  }

  .check-icon {
    font-size: 40px;
    flex-shrink: 0;
  }

  .check-content {
    flex: 1;
  }

  .check-title {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--text-main);
    margin: 0 0 var(--space-2) 0;
  }

  .check-description {
    font-size: var(--font-size-base);
    color: var(--text-muted);
    margin: 0;
    line-height: 1.5;
  }

  .backup-download {
    text-align: center;
    padding: var(--space-8);
    background: rgba(255, 255, 255, 0.03);
    border-radius: var(--border-radius-xl);
    border: 1px solid rgba(255, 255, 255, 0.1);
    max-width: 500px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .backup-text {
    font-size: var(--font-size-base);
    color: var(--text-muted);
    margin-bottom: var(--space-4);
  }

  .btn-backup {
    min-width: 280px;
  }

  /* ========================================
     WHAT'S INCLUDED SECTION
     ======================================== */
  .whats-included {
    background: linear-gradient(to bottom, #1e1b4b, var(--bg-dark));
  }

  .included-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    max-width: 900px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .included-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .included-card {
    background: var(--bg-card);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius-xl);
    padding: var(--space-8);
    text-align: center;
    transition: all 0.3s ease;
  }

  .included-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-teal);
    box-shadow: 0 10px 30px rgba(20, 184, 166, 0.2);
  }

  .card-number {
    display: inline-block;
    width: 60px;
    height: 60px;
    background: rgba(20, 184, 166, 0.2);
    color: var(--accent-teal);
    font-size: var(--font-size-2xl);
    font-weight: 800;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-4);
    border: 2px solid var(--accent-teal);
  }

  .card-title {
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--text-main);
    margin-bottom: var(--space-3);
  }

  .card-description {
    font-size: var(--font-size-base);
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* ========================================
     NEXT STEP SECTION
     ======================================== */
  .next-step {
    background: var(--bg-dark);
    position: relative;
  }

  .next-step::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 50% 50%,
      rgba(234, 179, 8, 0.1),
      transparent 70%
    );
    pointer-events: none;
  }

  .next-step-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    background: var(--bg-card);
    padding: var(--space-12);
    border-radius: var(--border-radius-2xl);
    border: 2px solid rgba(234, 179, 8, 0.3);
    position: relative;
    z-index: 1;
  }

  .step-badge {
    display: inline-block;
    background: rgba(234, 179, 8, 0.2);
    color: var(--accent-gold);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--border-radius-full);
    font-size: var(--font-size-sm);
    font-weight: 600;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-4);
  }

  .step-title {
    font-size: var(--font-size-3xl);
    font-weight: 800;
    margin-bottom: var(--space-4);
    color: var(--text-main);
  }

  .step-description {
    font-size: var(--font-size-lg);
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-8);
  }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-bottom: var(--space-8);
    text-align: left;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .benefit-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: var(--font-size-base);
    color: var(--text-muted);
  }

  .benefit-icon {
    color: var(--accent-gold);
    font-weight: bold;
    font-size: var(--font-size-lg);
    flex-shrink: 0;
  }

  .step-ctas {
    margin-bottom: var(--space-4);
  }

  .btn-step {
    min-width: 300px;
  }

  .btn-gold {
    background: var(--accent-gold) !important;
    color: black !important;
    font-weight: bold;
    border: none;
    box-shadow: 0 0 20px rgba(234, 179, 8, 0.3);
  }

  .btn-gold:hover {
    background: #facc15 !important;
    box-shadow: 0 0 40px rgba(234, 179, 8, 0.6);
  }

  .step-note {
    font-size: var(--font-size-sm);
    color: var(--text-muted);
  }

  /* ========================================
     SOCIAL SHARE SECTION
     ======================================== */
  .social-share {
    background: linear-gradient(to bottom, var(--bg-dark), #000000);
  }

  .share-content {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }

  .share-title {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--text-main);
  }

  .share-description {
    font-size: var(--font-size-base);
    color: var(--text-muted);
    margin-bottom: var(--space-8);
  }

  .share-buttons {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    align-items: center;
  }

  @media (min-width: 640px) {
    .share-buttons {
      flex-direction: row;
      justify-content: center;
    }
  }

  .share-button {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    border-radius: var(--border-radius-lg);
    font-size: var(--font-size-base);
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: var(--bg-card);
    color: var(--text-main);
    min-width: 220px;
    justify-content: center;
  }

  .share-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
  }

  .share-button.twitter:hover {
    border-color: #1da1f2;
    box-shadow: 0 10px 25px rgba(29, 161, 242, 0.3);
  }

  .share-button.linkedin:hover {
    border-color: #0077b5;
    box-shadow: 0 10px 25px rgba(0, 119, 181, 0.3);
  }

  .share-button.facebook:hover {
    border-color: #1877f2;
    box-shadow: 0 10px 25px rgba(24, 119, 242, 0.3);
  }

  .share-icon {
    font-size: var(--font-size-xl);
    font-weight: bold;
  }

  /* ========================================
     RESPONSIVE
     ======================================== */
  @media (max-width: 768px) {
    .title {
      font-size: var(--font-size-3xl);
    }

    .subtitle {
      font-size: var(--font-size-lg);
    }

    .section-title {
      font-size: var(--font-size-2xl);
    }

    .step-title {
      font-size: var(--font-size-2xl);
    }

    .share-title {
      font-size: var(--font-size-xl);
    }

    .success-icon {
      font-size: 80px;
    }

    .btn-step {
      min-width: 250px;
    }
  }
</style>

<script>
  // ========================================
  // INTERSECTION OBSERVER - ANIMATIONS
  // ========================================
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const delay = entry.target.getAttribute('data-delay') || 0;
        setTimeout(() => {
          entry.target.classList.add('animated');
        }, parseInt(delay));
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.querySelectorAll('.animate-on-scroll').forEach(el => {
    observer.observe(el);
  });

  // ========================================
  // ANALYTICS TRACKING
  // ========================================
  function trackEvent(eventName: string, eventData = {}) {
    // Google Analytics 4
    if (typeof window !== 'undefined' && 'gtag' in window) {
      (window as any).gtag('event', eventName, eventData);
    }

    // Plausible Analytics
    if (typeof window !== 'undefined' && 'plausible' in window) {
      (window as any).plausible(eventName, { props: eventData });
    }

    // Facebook Pixel
    if (typeof window !== 'undefined' && 'fbq' in window) {
      (window as any).fbq('track', eventName, eventData);
    }

    // Console log for debugging
    console.log('Track Event:', eventName, eventData);
  }

  // Track page view
  trackEvent('thank_you_page_view', {
    page: 'recursos-gratis-gracias',
  });

  // Track CTA clicks
  document.querySelectorAll('[data-track-event]').forEach(button => {
    button.addEventListener('click', e => {
      const eventName = (e.currentTarget as HTMLElement).getAttribute(
        'data-track-event'
      );
      if (eventName) {
        trackEvent(eventName, {
          button_text: (e.currentTarget as HTMLElement).textContent?.trim(),
        });
      }
    });
  });
</script>
