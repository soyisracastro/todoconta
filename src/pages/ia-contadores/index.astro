---
import LandingLayout from '@/layouts/LandingLayout.astro';
import Button from '@/components/ui/Button.astro';

const title = 'Masterclass GRATIS - Por Qué el 90% de los Contadores Usa IA de Forma Incorrecta';
const description =
  'Descubre el framework que te permitirá usar ChatGPT, Claude y Gemini 3 como estratega. 45 minutos que cambiarán tu perspectiva sobre IA.';
const keywords = [
  'masterclass IA contadores',
  'ChatGPT para contadores',
  'Claude AI',
  'Gemini 3',
  'IA para contabilidad',
  'curso gratuito IA',
  'webinar contadores',
];

// Configuration
const INSTRUCTOR_NAME = 'Israel Castro';

// CTA Button Text Options (randomly selected)
const ctaOptions = [
  '🎯 Reservar mi lugar',
  '🚀 Quiero asistir',
  '📚 Inscribirme gratis',
  '✅ Asegurar mi espacio',
  '👋 Apartar mi lugar',
];
const selectedCTA = ctaOptions[Math.floor(Math.random() * ctaOptions.length)];

// Testimonials
const testimonials = [
  {
    name: 'Ricardo M.',
    role: 'Contador con 8 años de experiencia, CDMX',
    avatar: '👤',
    quote:
      'Después de aplicar lo que aprendí en el taller completo, ahora combino Claude y ChatGPT estratégicamente para dictámenes fiscales. He reducido significativamente el tiempo sin comprometer calidad.',
    highlight: 'Resultados del taller de 12 días',
  },
  {
    name: 'Lizeth G.',
    role: 'Despacho contable, Guadalajara',
    avatar: '👤',
    quote:
      'Después del taller, cambié mi forma de trabajar. Ahora uso la herramienta correcta para cada tarea y he podido aumentar mi capacidad de atención sin contratar más personal.',
    highlight: 'Resultados del taller de 12 días',
  },
  {
    name: 'Carmen Lucía R.',
    role: 'Director de despacho con 5 contadores, Monterrey',
    avatar: '👤',
    quote:
      'Lo más valioso fue cambiar mi mentalidad sobre IA. Después del taller completo, implementé el framework con mi equipo, y ahora usamos IA como asistente inteligente en declaraciones complementarias y análisis de discrepancias.',
    highlight: 'Resultados del taller de 12 días',
  },
];
---

<LandingLayout title={title} description={description} keywords={keywords} showFooter={false}>
  <!-- HERO: MASTERCLASS GRATUITA -->
  <section class="hero">
    <div class="hero-gradient-bg"></div>
    <div class="floating-particles">
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
    </div>

    <div class="container">
      <div class="badge">
        <span class="badge-icon">🎁</span>
        <span class="badge-text">MASTERCLASS GRATUITA</span>
      </div>

      <h1 class="title animate-on-scroll" data-animation="fade-in-up" data-delay="100">
        "Por Qué el 90% de los Contadores<br />
        <span class="gradient gradient-animated">Usa IA de Forma Incorrecta"</span>
      </h1>

      <p class="subtitle animate-on-scroll" data-animation="fade-in-up" data-delay="200">
        Descubre el framework que te permitirá <strong>empezar a pensar</strong> con inteligencia artificial como
        <strong>estratega</strong>, no como usuario.
      </p>

      <div class="cta-primary animate-on-scroll" data-animation="scale-in" data-delay="300">
        <Button
          href="#eventos"
          variant="primary"
          size="xl"
          class="btn-gold btn-pulse scroll-to-events"
          data-track-event="cta_hero_scroll"
        >
          {selectedCTA}
        </Button>
        <p class="social-proof">
          👥 Más de 120 contadores ya registrados
        </p>
      </div>
    </div>
  </section>

  <!-- EVENTOS DISPONIBLES -->
  <section class="events" id="eventos">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">Elige tu fecha</h2>
        <p class="section-subtitle">Selecciona el evento que mejor se ajuste a tu agenda</p>
      </div>

      <div class="iframe-container animate-on-scroll" data-animation="scale-in" data-delay="200">
        <iframe
          src="https://luma.com/embed/calendar/cal-ZzeY9AdlGzPYMmZ/events?tag=IA"
          width="600"
          height="450"
          frameborder="0"
          style="border: 1px solid #bfcbda88; border-radius: 4px;"
          allowfullscreen=""
          aria-hidden="false"
          tabindex="0"
        ></iframe>
      </div>

      <p class="events-note animate-on-scroll" data-animation="fade-in" data-delay="400">
        💡 <strong>Tip:</strong> Si no puedes asistir en vivo, recibirás el replay por 48 horas
      </p>
    </div>
  </section>

  <!-- LO QUE APRENDERÁS -->
  <section class="learning">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">En 45 minutos descubrirás:</h2>
      </div>

      <div class="learning-grid">
        <div class="learning-item animate-on-scroll" data-animation="slide-in-left" data-delay="0">
          <span class="check">✅</span>
          <p>Los <strong>3 errores</strong> que cometen el 90% de contadores con IA</p>
        </div>
        <div class="learning-item animate-on-scroll" data-animation="slide-in-left" data-delay="100">
          <span class="check">✅</span>
          <p>El <strong>framework IA-First</strong> de 3 preguntas (funciona con cualquier IA)</p>
        </div>
        <div class="learning-item animate-on-scroll" data-animation="slide-in-left" data-delay="200">
          <span class="check">✅</span>
          <p>Por qué <strong>depender solo de ChatGPT</strong> te limita</p>
        </div>
        <div class="learning-item animate-on-scroll" data-animation="slide-in-left" data-delay="300">
          <span class="check">✅</span>
          <p>
            <strong>Demo en vivo:</strong> Caso fiscal resuelto en 12 min (vs 2 horas tradicionales)
          </p>
        </div>
        <div class="learning-item animate-on-scroll" data-animation="slide-in-left" data-delay="400">
          <span class="check">✅</span>
          <p>
            <strong>Matriz de decisión:</strong> Cuándo usar ChatGPT, Claude o Gemini 3
          </p>
        </div>
      </div>

      <div class="bonus-badge animate-on-scroll" data-animation="scale-in" data-delay="500">
        🎁 <strong>BONUS:</strong> Los asistentes recibirán acceso a recursos exclusivos y un descuento
        especial.
      </div>
    </div>
  </section>

  <!-- TESTIMONIOS -->
  <section class="testimonials">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">Lo que dicen contadores como tú</h2>
        <p class="section-subtitle">Resultados reales de talleres anteriores</p>
      </div>

      <div class="testimonials-grid">
        {
          testimonials.map((testimonial, index) => (
            <div
              class="testimonial-card animate-on-scroll"
              data-animation="fade-in-up"
              data-delay={index * 150}
            >
              <div class="quote-icon">"</div>
              <p class="testimonial-quote">{testimonial.quote}</p>
              <div class="testimonial-highlight">{testimonial.highlight}</div>
              <div class="testimonial-author">
                <div class="author-avatar">{testimonial.avatar}</div>
                <div class="author-info">
                  <p class="author-name">{testimonial.name}</p>
                  <p class="author-role">{testimonial.role}</p>
                </div>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- PARA QUIÉN ES -->
  <section class="audience">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">✅ Esta masterclass ES para ti si:</h2>
      </div>

      <div class="audience-list">
        <div class="audience-item animate-on-scroll" data-animation="slide-in-right" data-delay="0">
          <span class="bullet">•</span>
          <p>Eres <strong>contador público en México</strong></p>
        </div>
        <div
          class="audience-item animate-on-scroll"
          data-animation="slide-in-right"
          data-delay="100"
        >
          <span class="bullet">•</span>
          <p>Ya usas IA pero sientes que la aprovechas al <strong>10%</strong></p>
        </div>
        <div
          class="audience-item animate-on-scroll"
          data-animation="slide-in-right"
          data-delay="200"
        >
          <span class="bullet">•</span>
          <p>Te abruma la <strong>cantidad de herramientas</strong> que existen</p>
        </div>
        <div
          class="audience-item animate-on-scroll"
          data-animation="slide-in-right"
          data-delay="300"
        >
          <span class="bullet">•</span>
          <p>Quieres <strong>diferenciarte de la competencia</strong></p>
        </div>
        <div
          class="audience-item animate-on-scroll"
          data-animation="slide-in-right"
          data-delay="400"
        >
          <span class="bullet">•</span>
          <p>Buscas <strong>aumentar tus ingresos</strong> con IA</p>
        </div>
      </div>
    </div>
  </section>

  <!-- POR QUÉ GRATIS -->
  <section class="why-free">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">💡 ¿Por qué compartir esto sin costo?</h2>
      </div>

      <div class="why-content">
        <p class="intro animate-on-scroll" data-animation="fade-in" data-delay="100">
          Porque he visto cómo la mayoría de contadores usa IA de forma limitada:
        </p>

        <div class="problems">
          <div class="problem animate-on-scroll" data-animation="slide-in-left" data-delay="0">
            <span class="icon">❌</span>
            <p>Solo ChatGPT gratis (sin saber que hay mejores opciones)</p>
          </div>
          <div class="problem animate-on-scroll" data-animation="slide-in-left" data-delay="100">
            <span class="icon">❌</span>
            <p>Prompts genéricos que dan respuestas mediocres</p>
          </div>
          <div class="problem animate-on-scroll" data-animation="slide-in-left" data-delay="200">
            <span class="icon">❌</span>
            <p>Sin estrategia para elegir la herramienta correcta</p>
          </div>
        </div>

        <p class="mission animate-on-scroll" data-animation="fade-in-up" data-delay="300">
          Mi misión es cambiar eso. Esta masterclass es el <strong>primer paso</strong> para desarrollar
          tu Mentalidad IA-First.
        </p>

        <p class="decision animate-on-scroll" data-animation="scale-in" data-delay="400">
          Después, <strong>tú decides</strong> si quieres profundizar más.
        </p>
      </div>
    </div>
  </section>

  <!-- SOBRE EL INSTRUCTOR -->
  <section class="instructor">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">Sobre el Instructor</h2>
      </div>

      <div class="instructor-card animate-on-scroll" data-animation="scale-in" data-delay="200">
        <img
          src="/images/photo.png"
          alt={INSTRUCTOR_NAME}
          class="avatar"
        />
        <h3 class="name">{INSTRUCTOR_NAME}</h3>
        <p class="role">Contador público y programador con 12+ años de experiencia</p>
        <p class="credentials">Creador de TodoConta</p>
        <p class="bio">
          A través de mi plataforma, he trabajado con <strong>cientos de contadores mexicanos</strong> interesados en integrar IA. Los que implementan el framework reportan mejoras significativas en productividad y nuevas oportunidades de ingresos.
        </p>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq">
    <div class="container">
      <div class="section-header animate-on-scroll" data-animation="fade-in-up">
        <h2 class="section-title">Preguntas Frecuentes</h2>
      </div>

      <div class="faq-list">
        <details class="faq-item animate-on-scroll" data-animation="fade-in-up" data-delay="0">
          <summary class="faq-question">
            ¿Necesito experiencia previa en IA?
            <span class="arrow">▼</span>
          </summary>
          <div class="faq-answer">
            <p>
              No. Esta masterclass es para cualquier nivel. Ya sea que nunca hayas usado IA o que ya
              uses ChatGPT ocasionalmente, encontrarás valor en el contenido.
            </p>
          </div>
        </details>

        <details class="faq-item animate-on-scroll" data-animation="fade-in-up" data-delay="100">
          <summary class="faq-question">
            ¿Tendré que pagar por ChatGPT Plus, Claude Pro o Gemini Pro?
            <span class="arrow">▼</span>
          </summary>
          <div class="faq-answer">
            <p>
              No. Te mostraré cómo aprovechar las versiones gratuitas de ChatGPT, Claude y Gemini 3.
              Si ya tienes cuentas de pago, mejor aún, pero no son necesarias.
            </p>
          </div>
        </details>

        <details class="faq-item animate-on-scroll" data-animation="fade-in-up" data-delay="200">
          <summary class="faq-question">
            ¿Quedará grabación?
            <span class="arrow">▼</span>
          </summary>
          <div class="faq-answer">
            <p>
              Sí, los registrados recibirán el replay por 48 horas. Sin embargo, te recomiendo asistir
              en vivo para participar en la sesión de preguntas y respuestas.
            </p>
          </div>
        </details>

        <details class="faq-item animate-on-scroll" data-animation="fade-in-up" data-delay="300">
          <summary class="faq-question">
            ¿Habrá venta al final?
            <span class="arrow">▼</span>
          </summary>
          <div class="faq-answer">
            <p>
              Te compartiré información sobre un <a href="/ia-contadores/reto-12-dias">reto de 12 días</a>, pero SIN presión. La
              masterclass tiene valor por sí misma y no estás obligado a comprar nada.
            </p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- CTA FINAL -->
  <section class="cta-final">
    <div class="stars-bg"></div>
    <div class="container">
      <h2 class="final-title animate-on-scroll" data-animation="fade-in-up">
        No dejes pasar esta oportunidad
      </h2>

      <div class="final-cta animate-on-scroll" data-animation="scale-in" data-delay="200">
        <Button
          href="#eventos"
          variant="primary"
          size="xl"
          class="btn-gold btn-pulse scroll-to-events"
          data-track-event="cta_final_scroll"
        >
          {selectedCTA}
        </Button>

        <div class="urgency">
          <p class="date animate-on-scroll" data-animation="fade-in" data-delay="400">
            ⏰ Nuevas fechas disponibles
          </p>
          <p class="date animate-on-scroll" data-animation="fade-in" data-delay="450">
            👥 Más de 120 contadores ya registrados
          </p>
          <p class="date animate-on-scroll" data-animation="fade-in" data-delay="500">
            📹 Quedará grabación disponible 48h
          </p>
        </div>
      </div>
    </div>
  </section>
</LandingLayout>

<style>
  /* ========================================
     VARIABLES Y GLOBALS
     ======================================== */
  :root {
    --bg-dark: #0f0518;
    --bg-darker: #000000;
    --bg-card: rgba(255, 255, 255, 0.05);
    --text-main: #ffffff;
    --text-muted: #9ca3af;
    --accent-purple: #a855f7;
    --accent-gold: #eab308;
    --color-success: #10b981;
  }

  section {
    padding: var(--space-16) 0;
    color: var(--text-main);
    position: relative;
    overflow: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-4);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .section-title {
    font-size: var(--font-size-3xl);
    font-weight: 800;
    line-height: 1.2;
    color: #ffffff !important;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
    opacity: 1 !important;
    background: none !important;
    -webkit-background-clip: unset !important;
    -webkit-text-fill-color: #ffffff !important;
    background-clip: unset !important;
  }

  .section-subtitle {
    font-size: var(--font-size-xl);
    color: var(--text-muted);
    margin-top: var(--space-4);
  }

  /* ========================================
     ANIMATIONS
     ======================================== */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes scaleIn {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(1);
      box-shadow: 0 0 20px rgba(234, 179, 8, 0.3);
    }
    50% {
      transform: scale(1.02);
      box-shadow: 0 0 30px rgba(234, 179, 8, 0.5);
    }
  }

  @keyframes gradientShift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  @keyframes particleFloat {
    0% {
      transform: translate(0, 0) rotate(0deg);
    }
    33% {
      transform: translate(30px, -50px) rotate(120deg);
    }
    66% {
      transform: translate(-20px, 20px) rotate(240deg);
    }
    100% {
      transform: translate(0, 0) rotate(360deg);
    }
  }

  /* Animation classes */
  .animate-on-scroll {
    opacity: 0;
  }

  .animate-on-scroll.animated {
    animation-duration: 0.8s;
    animation-fill-mode: forwards;
    animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  .animated[data-animation='fade-in-up'] {
    animation-name: fadeInUp;
  }
  .animated[data-animation='fade-in-down'] {
    animation-name: fadeInDown;
  }
  .animated[data-animation='fade-in'] {
    animation-name: fadeIn;
  }
  .animated[data-animation='slide-in-left'] {
    animation-name: slideInLeft;
  }
  .animated[data-animation='slide-in-right'] {
    animation-name: slideInRight;
  }
  .animated[data-animation='scale-in'] {
    animation-name: scaleIn;
  }

  /* ========================================
     HERO SECTION
     ======================================== */
  .hero {
    background: linear-gradient(to bottom, #000000, #1e1b4b);
    min-height: 100vh;
    display: flex;
    align-items: center;
    text-align: center;
    position: relative;
    border-radius: 0;
  }

  .hero-gradient-bg {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 50% 0%,
      rgba(168, 85, 247, 0.2),
      transparent 50%
    );
    pointer-events: none;
  }

  .floating-particles {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .particle {
    position: absolute;
    width: 4px;
    height: 4px;
    background: var(--accent-purple);
    border-radius: 50%;
    opacity: 0.3;
    animation: particleFloat 15s infinite ease-in-out;
  }

  .particle:nth-child(1) {
    top: 10%;
    left: 20%;
    animation-delay: 0s;
  }
  .particle:nth-child(2) {
    top: 20%;
    right: 30%;
    animation-delay: 2s;
  }
  .particle:nth-child(3) {
    top: 60%;
    left: 10%;
    animation-delay: 4s;
  }
  .particle:nth-child(4) {
    top: 40%;
    right: 20%;
    animation-delay: 1s;
  }
  .particle:nth-child(5) {
    bottom: 20%;
    left: 50%;
    animation-delay: 3s;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: rgba(255, 255, 255, 0.1);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--border-radius-full);
    border: 1px solid rgba(255, 255, 255, 0.2);
    margin-bottom: var(--space-8);
    animation: float 3s infinite ease-in-out;
    backdrop-filter: blur(10px);
  }

  .badge-text {
    color: #e9d5ff;
    font-weight: 500;
    font-size: var(--font-size-sm);
  }

  .title {
    font-size: var(--font-size-5xl);
    font-weight: 800;
    margin-bottom: var(--space-6);
    line-height: 1.1;
    color: #ffffff;
    position: relative;
    z-index: 1;
  }

  .gradient {
    background: linear-gradient(to right, #60a5fa, #c084fc, #f472b6);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent;
    display: inline-block;
  }

  .gradient-animated {
    background: linear-gradient(
      90deg,
      #60a5fa,
      #c084fc,
      #f472b6,
      #60a5fa,
      #c084fc
    );
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    animation: gradientShift 5s linear infinite;
    display: inline-block;
  }

  .subtitle {
    font-size: var(--font-size-xl);
    color: #e2e8f0;
    max-width: 700px;
    margin: 0 auto var(--space-16);
    line-height: 1.6;
    position: relative;
    z-index: 1;
  }

  /* Countdown Timer */
  .countdown-container {
    margin: var(--space-8) auto;
    max-width: 600px;
    position: relative;
    z-index: 1;
  }

  .countdown-label {
    font-size: var(--font-size-lg);
    color: var(--accent-gold);
    font-weight: 600;
    margin-bottom: var(--space-4);
    text-align: center;
  }

  .countdown {
    display: flex;
    justify-content: center;
    gap: var(--space-2);
    align-items: center;
    flex-wrap: wrap;
  }

  .countdown-item {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius-lg);
    padding: var(--space-4);
    min-width: 80px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-1);
    transition: all 0.3s ease;
  }

  .countdown-item:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .countdown-number {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--accent-gold);
    font-variant-numeric: tabular-nums;
    line-height: 1;
  }

  .countdown-text {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .countdown-separator {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    color: var(--accent-purple);
    margin: 0 var(--space-1);
  }

  .meta {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
  }

  .meta-item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    background: rgba(0, 0, 0, 0.3);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--border-radius-lg);
    color: #e2e8f0;
    backdrop-filter: blur(4px);
    transition: all 0.3s ease;
  }

  .meta-item:hover {
    background: rgba(0, 0, 0, 0.5);
    transform: translateY(-2px);
  }

  .benefit {
    font-size: var(--font-size-lg);
    color: var(--text-muted);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    transition: all 0.3s ease;
  }

  .benefit:hover {
    color: var(--text-main);
    transform: scale(1.05);
  }

  .cta-primary {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    position: relative;
    z-index: 1;
  }

  .btn-gold {
    background: var(--accent-gold) !important;
    color: black !important;
    font-weight: bold;
    border: none;
    box-shadow: 0 0 20px rgba(234, 179, 8, 0.3);
    transition: all 0.3s ease;
  }

  .btn-gold:hover {
    transform: scale(1.05);
    background: #facc15 !important;
    box-shadow: 0 0 40px rgba(234, 179, 8, 0.6);
  }

  .btn-pulse {
    animation: pulse 2s infinite;
  }

  .social-proof {
    font-size: var(--font-size-sm);
    color: var(--text-muted);
  }

  /* ========================================
     EVENTS SECTION
     ======================================== */
  .events {
    background: linear-gradient(to bottom, #1e1b4b, var(--bg-dark));
    padding: var(--space-20) 0;
  }

  .iframe-container {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
    max-width: 100%;
    padding: var(--space-8);
  }

  .iframe-container iframe {
    max-width: 100%;
    width: 600px;
    height: 450px;
    border: 1px solid rgba(168, 85, 247, 0.3) !important;
    border-radius: var(--border-radius-xl) !important;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
    transition: all 0.3s ease;
  }

  .iframe-container iframe:hover {
    box-shadow: 0 20px 80px rgba(168, 85, 247, 0.6), 0 0 40px rgba(168, 85, 247, 0.3);
    transform: translateY(-8px);
  }

  .events-note {
    text-align: center;
    margin-top: var(--space-8);
    font-size: var(--font-size-sm);
    color: var(--text-muted);
    background: rgba(255, 255, 255, 0.05);
    padding: var(--space-4);
    border-radius: var(--border-radius-lg);
    border: 1px solid rgba(168, 85, 247, 0.2);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  /* ========================================
     LEARNING SECTION
     ======================================== */
  .learning {
    background: var(--bg-dark);
  }

  .learning-grid {
    max-width: 800px;
    margin: 0 auto var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .learning-item {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
    background: var(--bg-card);
    padding: var(--space-4);
    border-radius: var(--border-radius-lg);
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
  }

  .learning-item:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateX(10px);
    border-color: var(--accent-purple);
  }

  .learning-item .check {
    font-size: var(--font-size-xl);
    flex-shrink: 0;
  }

  .learning-item p {
    margin: 0;
    font-size: var(--font-size-lg);
    line-height: 1.6;
  }

  .bonus-badge {
    background: linear-gradient(
      to right,
      rgba(168, 85, 247, 0.2),
      rgba(234, 179, 8, 0.2)
    );
    padding: var(--space-4);
    border-radius: var(--border-radius-lg);
    border: 1px solid rgba(168, 85, 247, 0.3);
    text-align: center;
    font-size: var(--font-size-sm);
    max-width: 800px;
    margin: 0 auto;
    transition: all 0.3s ease;
  }

  .bonus-badge:hover {
    transform: scale(1.02);
    box-shadow: 0 10px 30px rgba(168, 85, 247, 0.3);
  }

  /* ========================================
     TESTIMONIALS SECTION
     ======================================== */
  .testimonials {
    background: linear-gradient(to bottom, var(--bg-dark), #1a0b2e);
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    max-width: 1000px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .testimonials-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .testimonial-card {
    background: var(--bg-card);
    padding: var(--space-6);
    border-radius: var(--border-radius-xl);
    border: 1px solid rgba(255, 255, 255, 0.1);
    position: relative;
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-10px);
    border-color: var(--accent-purple);
    box-shadow: 0 20px 40px rgba(168, 85, 247, 0.3);
  }

  .quote-icon {
    font-size: 60px;
    color: var(--accent-purple);
    opacity: 0.2;
    line-height: 1;
    margin-bottom: var(--space-2);
  }

  .testimonial-quote {
    font-size: var(--font-size-base);
    line-height: 1.6;
    color: var(--text-muted);
    margin-bottom: var(--space-4);
  }

  .testimonial-highlight {
    background: rgba(168, 85, 247, 0.2);
    color: var(--accent-purple);
    padding: var(--space-2) var(--space-3);
    border-radius: var(--border-radius-lg);
    font-size: var(--font-size-sm);
    font-weight: 600;
    margin-bottom: var(--space-4);
    text-align: center;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding-top: var(--space-4);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .author-avatar {
    font-size: 25px;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
  }

  .author-name {
    font-weight: 600;
    margin: 0;
    font-size: var(--font-size-base);
  }

  .author-role {
    font-size: var(--font-size-sm);
    color: var(--text-main);
    margin: 0;
  }

  /* ========================================
     AUDIENCE SECTION
     ======================================== */
  .audience {
    background: #1a0b2e;
  }

  .audience-list {
    max-width: 700px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .audience-item {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
    font-size: var(--font-size-xl);
    transition: all 0.3s ease;
  }

  .audience-item:hover {
    transform: translateX(10px);
  }

  .audience-item .bullet {
    color: var(--accent-purple);
    font-size: var(--font-size-2xl);
    flex-shrink: 0;
  }

  .audience-item p {
    margin: 0;
    line-height: 1.6;
  }

  /* ========================================
     WHY FREE SECTION
     ======================================== */
  .why-free {
    background: var(--bg-dark);
  }

  .why-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .why-content .intro {
    font-size: var(--font-size-xl);
    text-align: center;
    margin-bottom: var(--space-8);
    color: var(--text-muted);
  }

  .problems {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  .problem {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
    background: var(--bg-card);
    padding: var(--space-4);
    border-radius: var(--border-radius-lg);
    border: 1px solid rgba(239, 68, 68, 0.3);
    transition: all 0.3s ease;
  }

  .problem:hover {
    transform: translateX(10px);
    border-color: rgba(239, 68, 68, 0.6);
  }

  .problem .icon {
    font-size: var(--font-size-xl);
    flex-shrink: 0;
  }

  .problem p {
    margin: 0;
    font-size: var(--font-size-lg);
    line-height: 1.6;
  }

  .mission,
  .decision {
    font-size: var(--font-size-xl);
    text-align: center;
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .mission {
    color: var(--text-main);
  }

  .decision {
    color: var(--accent-purple);
    font-weight: 500;
  }

  /* ========================================
     INSTRUCTOR SECTION
     ======================================== */
  .instructor {
    background: linear-gradient(to bottom, var(--bg-dark), #1a0b2e);
  }

  .instructor-card {
    max-width: 600px;
    margin: 0 auto;
    background: var(--bg-card);
    padding: var(--space-8);
    border-radius: var(--border-radius-xl);
    border: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
    transition: all 0.3s ease;
  }

  .instructor-card:hover {
    border-color: var(--accent-purple);
    box-shadow: 0 20px 60px rgba(168, 85, 247, 0.3);
    transform: translateY(-5px);
  }

  .avatar {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto var(--space-4);
    border: 3px solid var(--accent-gold);
    box-shadow: 0 8px 24px rgba(234, 179, 8, 0.2);
  }

  .name {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-2);
    color: var(--accent-gold)
  }

  .role,
  .credentials {
    font-size: var(--font-size-lg);
    color: var(--text-muted);
    margin-bottom: var(--space-2);
  }

  .bio {
    font-size: var(--font-size-lg);
    line-height: 1.6;
    margin-top: var(--space-4);
    color: var(--text-main);
  }

  /* ========================================
     FAQ SECTION
     ======================================== */
  .faq {
    background: #1a0b2e;
  }

  .faq-list {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .faq-item {
    background: var(--bg-card);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius-xl);
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .faq-item:hover {
    border-color: var(--accent-purple);
  }

  .faq-question {
    padding: var(--space-6);
    font-weight: 600;
    font-size: var(--font-size-lg);
    color: #ffffff;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
    user-select: none;
    transition: all 0.3s ease;
  }

  .faq-question:hover {
    background: rgba(255, 255, 255, 0.03);
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-answer {
    padding: 0 var(--space-6) var(--space-6) var(--space-6);
    color: var(--text-muted);
    line-height: 1.6;
  }

  .faq-answer p {
    margin: 0;
  }

  .faq-item[open] .faq-question {
    background: rgba(255, 255, 255, 0.05);
  }

  .faq-item[open] .arrow {
    transform: rotate(180deg);
  }

  .arrow {
    transition: transform 0.3s ease;
    color: var(--accent-purple);
  }

  /* ========================================
     CTA FINAL SECTION
     ======================================== */
  .cta-final {
    background: linear-gradient(180deg, #1a0b2e 0%, #0f0518 50%, #000000 100%);
    text-align: center;
    padding: var(--space-24) 0;
    position: relative;
    overflow: hidden;
  }

  .cta-final::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle at 50% 50%,
      rgba(168, 85, 247, 0.15),
      transparent 70%
    );
    pointer-events: none;
  }

  .stars-bg {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(
      2px 2px at 20px 30px,
      white,
      transparent
    ),
      radial-gradient(2px 2px at 60px 70px, white, transparent),
      radial-gradient(1px 1px at 50px 50px, white, transparent),
      radial-gradient(1px 1px at 130px 80px, white, transparent),
      radial-gradient(2px 2px at 90px 10px, white, transparent);
    background-size: 200px 200px;
    background-repeat: repeat;
    opacity: 0.4;
    animation: stars-drift 120s linear infinite;
  }

  @keyframes stars-drift {
    from {
      transform: translateY(0);
    }
    to {
      transform: translateY(-200px);
    }
  }

  .final-title {
    font-size: var(--font-size-4xl);
    font-weight: 800;
    margin-bottom: var(--space-10);
    position: relative;
    z-index: 1;
    color: #ffffff;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }

  .final-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-6);
    position: relative;
    z-index: 1;
  }

  .urgency {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    background: rgba(0, 0, 0, 0.4);
    padding: var(--space-4) var(--space-6);
    border-radius: var(--border-radius-xl);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(234, 179, 8, 0.2);
  }

  .spots,
  .date {
    font-size: var(--font-size-lg);
    color: #ffffff;
  }

  .spots {
    color: var(--accent-gold);
    font-weight: 700;
    font-size: var(--font-size-xl);
  }

  .date {
    color: rgba(255, 255, 255, 0.9);
    font-weight: 500;
  }

  /* ========================================
     RESPONSIVE
     ======================================== */
  @media (max-width: 768px) {
    .title {
      font-size: var(--font-size-3xl);
    }

    .subtitle {
      font-size: var(--font-size-lg);
    }

    .section-title {
      font-size: var(--font-size-2xl);
    }

    .final-title {
      font-size: var(--font-size-2xl);
    }

    .learning-item p,
    .audience-item,
    .problem p {
      font-size: var(--font-size-base);
    }

    .iframe-container iframe {
      width: 100%;
      height: 600px;
    }

    .events {
      padding: var(--space-12) 0;
    }

    .events-note {
      font-size: var(--font-size-base);
      margin-top: var(--space-6);
    }
  }
</style>

<script>
  // ========================================
  // INTERSECTION OBSERVER - ANIMATIONS
  // ========================================
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const delay = entry.target.getAttribute('data-delay') || 0;
        setTimeout(() => {
          entry.target.classList.add('animated');
        }, parseInt(delay));
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.querySelectorAll('.animate-on-scroll').forEach((el) => {
    observer.observe(el);
  });

  // ========================================
  // SMOOTH SCROLL TO EVENTS
  // ========================================
  document.querySelectorAll('.scroll-to-events').forEach((button) => {
    button.addEventListener('click', (e) => {
      e.preventDefault();
      const eventsSection = document.getElementById('eventos');
      if (eventsSection) {
        eventsSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });

  // ========================================
  // ANALYTICS TRACKING
  // ========================================
  function trackEvent(eventName, eventData = {}) {
    // Google Analytics 4
    if (typeof window !== 'undefined' && 'gtag' in window) {
      window.gtag('event', eventName, eventData);
    }

    // Plausible Analytics
    if (typeof window !== 'undefined' && 'plausible' in window) {
      window.plausible(eventName, { props: eventData });
    }

    // Facebook Pixel
    if (typeof window !== 'undefined' && 'fbq' in window) {
      window.fbq('track', eventName, eventData);
    }

    // Console log for debugging
    console.log('Track Event:', eventName, eventData);
  }

  // Track CTA clicks
  document.querySelectorAll('[data-track-event]').forEach((button) => {
    button.addEventListener('click', (e) => {
      const eventName = e.currentTarget.getAttribute('data-track-event');
      if (eventName) {
        trackEvent(eventName, {
          button_location: eventName.includes('hero') ? 'hero' : 'footer',
          button_text: e.currentTarget.textContent?.trim(),
        });
      }
    });
  });

  // Track scroll depth
  let maxScroll = 0;
  const scrollMilestones = [25, 50, 75, 100];
  const trackedMilestones = new Set();

  window.addEventListener('scroll', () => {
    const windowHeight = window.innerHeight;
    const documentHeight = document.documentElement.scrollHeight;
    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    const scrollPercent = (scrollTop / (documentHeight - windowHeight)) * 100;

    if (scrollPercent > maxScroll) {
      maxScroll = scrollPercent;

      scrollMilestones.forEach((milestone) => {
        if (scrollPercent >= milestone && !trackedMilestones.has(milestone)) {
          trackedMilestones.add(milestone);
          trackEvent('scroll_depth', {
            percent: milestone,
          });
        }
      });
    }
  });

  // Track FAQ interactions
  document.querySelectorAll('.faq-item').forEach((item) => {
    item.addEventListener('toggle', () => {
      if (item.hasAttribute('open')) {
        const question = item.querySelector('.faq-question')?.textContent?.trim();
        trackEvent('faq_opened', {
          question: question || 'unknown',
        });
      }
    });
  });

  // Track time on page
  const startTime = Date.now();
  window.addEventListener('beforeunload', () => {
    const timeOnPage = Math.round((Date.now() - startTime) / 1000);
    trackEvent('time_on_page', {
      seconds: timeOnPage,
    });
  });

  // Track section visibility
  const sectionObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting && entry.target instanceof HTMLElement) {
          const sectionName =
            entry.target.className.split(' ')[0] || 'unknown_section';
          trackEvent('section_viewed', {
            section: sectionName,
          });
          sectionObserver.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.5 }
  );

  document.querySelectorAll('section').forEach((section) => {
    sectionObserver.observe(section);
  });
</script>
