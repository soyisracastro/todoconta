---
// src/pages/servicio-simple/[slug].astro
import { getCollection } from 'astro:content';

// Genera rutas para cada servicio
export async function getStaticPaths() {
  const services = await getCollection('services');
  return services.map(service => ({
    params: { slug: service.slug },
    props: { service }
  }));
}

const { service } = Astro.props;
const { 
  title, 
  description, 
  monthlyPrice, 
  yearlyPrice,
  savings,
  priceNote,
  badge,
  featured,
  recommendedFor,
  heroImage,
  ctaText,
  ctaUrl,
  secondaryCtaText,
  secondaryCtaUrl,
  includedFeatures = [],
  categories = [],
  testimonials = [],
  faqs = [],
  process = [],
  stats = []
} = service.data;

// Formatear el precio
const formatPrice = (price) => {
  return new Intl.NumberFormat('es-MX', {
    style: 'currency',
    currency: 'MXN',
    maximumFractionDigits: 0
  }).format(price);
};

const formattedMonthlyPrice = formatPrice(monthlyPrice || 0);
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title} | Todoconta</title>
  <meta name="description" content={description}>
  
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --font-primary: 'Inter', sans-serif;
      --font-display: 'Plus Jakarta Sans', sans-serif;
      --primary-600: #0284c7;
      --primary-700: #0369a1;
      --green-500: #22c55e;
      --neutral-100: #f5f5f4;
      --neutral-200: #e7e5e4;
      --neutral-600: #57534e;
      --neutral-700: #44403c;
      --neutral-900: #1c1917;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: var(--font-primary);
      line-height: 1.6;
      color: var(--neutral-700);
      background-color: white;
    }
    
    img {
      max-width: 100%;
      height: auto;
    }
    
    a {
      color: var(--primary-600);
      text-decoration: none;
    }
    
    a:hover {
      color: var(--primary-700);
    }
    
    h1, h2, h3 {
      font-family: var(--font-display);
      color: var(--neutral-900);
      margin-bottom: 1rem;
    }
    
    h1 {
      font-size: 2.5rem;
      font-weight: 800;
    }
    
    h2 {
      font-size: 2rem;
      font-weight: 700;
    }
    
    p {
      margin-bottom: 1rem;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    
    .header {
      background-color: white;
      padding: 1rem 0;
      border-bottom: 1px solid var(--neutral-200);
    }
    
    .header .container {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .logo {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1.5rem;
    }
    
    .nav ul {
      display: flex;
      list-style: none;
      gap: 1.5rem;
    }
    
    .nav a {
      font-weight: 500;
    }
    
    .breadcrumbs {
      background-color: var(--neutral-100);
      padding: 0.75rem 0;
      border-bottom: 1px solid var(--neutral-200);
      margin-bottom: 2rem;
    }
    
    .breadcrumbs ul {
      display: flex;
      list-style: none;
      gap: 0.5rem;
    }
    
    .breadcrumbs li:not(:last-child)::after {
      content: '/';
      margin-left: 0.5rem;
      color: var(--neutral-600);
    }
    
    .hero {
      padding: 3rem 0;
    }
    
    .hero-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      align-items: center;
    }
    
    .hero-badge {
      display: inline-block;
      background-color: var(--primary-600);
      color: white;
      font-size: 0.75rem;
      font-weight: 600;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      margin-bottom: 1rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .hero-description {
      color: var(--neutral-600);
      margin-bottom: 1.5rem;
      font-size: 1.125rem;
    }
    
    .hero-recommended {
      background-color: var(--neutral-100);
      padding: 1rem;
      border-radius: 0.5rem;
      margin-bottom: 1.5rem;
    }
    
    .hero-actions {
      display: flex;
      gap: 1rem;
      margin-bottom: 2rem;
    }
    
    .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 600;
      font-family: var(--font-display);
      transition: all 0.2s;
    }
    
    .btn-primary {
      background-color: var(--primary-600);
      color: white;
    }
    
    .btn-primary:hover {
      background-color: var(--primary-700);
      color: white;
    }
    
    .btn-outline {
      border: 2px solid var(--primary-600);
      color: var(--primary-600);
    }
    
    .btn-outline:hover {
      background-color: var(--primary-600);
      color: white;
    }
    
    .hero-image {
      border-radius: 1rem;
      overflow: hidden;
    }
    
    .pricing {
      background-color: var(--neutral-100);
      padding: 4rem 0;
      margin: 3rem 0;
    }
    
    .pricing-title {
      text-align: center;
      margin-bottom: 2rem;
    }
    
    .pricing-card {
      background-color: white;
      border-radius: 1rem;
      padding: 2rem;
      max-width: 500px;
      margin: 0 auto;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }
    
    .price-amount {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 800;
      color: var(--primary-600);
      text-align: center;
      margin-bottom: 0.5rem;
    }
    
    .price-period {
      text-align: center;
      color: var(--neutral-600);
      margin-bottom: 2rem;
    }
    
    .features-list {
      list-style: none;
      margin-bottom: 2rem;
    }
    
    .feature-item {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--neutral-200);
    }
    
    .feature-item:last-child {
      border-bottom: none;
    }
    
    .feature-icon {
      color: var(--green-500);
    }
    
    .cta-button {
      display: block;
      background-color: var(--primary-600);
      color: white;
      text-align: center;
      padding: 1rem;
      border-radius: 0.5rem;
      font-weight: 600;
      font-family: var(--font-display);
    }
    
    .cta-button:hover {
      background-color: var(--primary-700);
      color: white;
    }
    
    .section-title {
      text-align: center;
      margin-bottom: 2rem;
    }
    
    .footer {
      background-color: var(--neutral-900);
      color: white;
      padding: 4rem 0 2rem;
      margin-top: 4rem;
    }
    
    .footer-bottom {
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      text-align: center;
      opacity: 0.7;
      font-size: 0.875rem;
    }
    
    @media (max-width: 768px) {
      .hero-content {
        grid-template-columns: 1fr;
      }
      
      .hero-actions {
        flex-direction: column;
      }
      
      .btn {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="container">
      <a href="/" class="logo">Todoconta</a>
      <nav class="nav">
        <ul>
          <li><a href="/servicios">Servicios</a></li>
          <li><a href="/tienda">Productos</a></li>
          <li><a href="/contacto">Contacto</a></li>
        </ul>
      </nav>
    </div>
  </header>
  
  <!-- Breadcrumbs -->
  <div class="breadcrumbs">
    <div class="container">
      <ul>
        <li><a href="/">Inicio</a></li>
        <li><a href="/servicios">Servicios</a></li>
        <li>{title}</li>
      </ul>
    </div>
  </div>
  
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-text">
          {badge && <div class="hero-badge">{badge}</div>}
          <h1 class="hero-title">{title}</h1>
          <p class="hero-description">{description}</p>
          
          {recommendedFor && (
            <div class="hero-recommended">
              <strong>Recomendado para:</strong> {recommendedFor}
            </div>
          )}
          
          <div class="hero-actions">
            <a href={ctaUrl || "#pricing"} class="btn btn-primary">
              {ctaText || "Contratar Ahora"}
            </a>
            
            {secondaryCtaText && secondaryCtaUrl && (
              <a href={secondaryCtaUrl} class="btn btn-outline">
                {secondaryCtaText}
              </a>
            )}
          </div>
        </div>
        
        <div class="hero-image">
          <img src={heroImage || "/images/default-service.jpg"} alt={title}>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Pricing Section -->
  <section class="pricing" id="pricing">
    <div class="container">
      <h2 class="pricing-title">Inversión</h2>
      
      <div class="pricing-card">
        <div class="price-amount">{formattedMonthlyPrice}</div>
        <div class="price-period">{priceNote || "mensual"}</div>
        
        {includedFeatures && includedFeatures.length > 0 && (
          <ul class="features-list">
            {includedFeatures.filter(f => f.included).map(feature => (
              <li class="feature-item">
                <span class="feature-icon">{feature.icon}</span>
                <span>{feature.text}</span>
              </li>
            ))}
          </ul>
        )}
        
        <a href={ctaUrl || "#"} class="cta-button">
          {ctaText || "Contratar Ahora"}
        </a>
      </div>
    </div>
  </section>
  
  <!-- Categories Section (simplificado) -->
  {categories && categories.length > 0 && (
    <section class="categories">
      <div class="container">
        <h2 class="section-title">¿Qué Incluye Nuestro Servicio?</h2>
        
        <div style="margin-top: 2rem; text-align: center;">
          <p>Este servicio incluye {categories.length} categorías de características.</p>
          <p>Encuentra todos los detalles en nuestra versión completa del sitio.</p>
        </div>
      </div>
    </section>
  )}
  
  <!-- Testimonials (simplificado) -->
  {testimonials && testimonials.length > 0 && (
    <section class="testimonials" style="background-color: #f0f9ff; padding: 4rem 0; margin: 3rem 0;">
      <div class="container">
        <h2 class="section-title">Lo Que Dicen Nuestros Clientes</h2>
        
        <div style="margin-top: 2rem; text-align: center;">
          <p>Contamos con {testimonials.length} testimonios de clientes satisfechos.</p>
          <p>Conoce sus experiencias en nuestra versión completa del sitio.</p>
        </div>
      </div>
    </section>
  )}
  
  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <!-- Contenido del footer -->
      </div>
      
      <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} Todoconta. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</body>
</html>