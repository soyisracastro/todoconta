---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/ui/Navbar.astro';
import Footer from '../components/ui/Footer.astro';

const menuLinks = [
  { title: 'Inicio', url: '/' },
];

const serviciosPopulares = [
  { 
    title: 'Inscripción RFC', 
    description: 'Obtén tu RFC de forma rápida y segura',
    url: '/servicios/inscripcion-rfc',
    icon: '📋'
  },
  { 
    title: 'Declaración Anual', 
    description: 'Cumple con tus obligaciones fiscales',
    url: '/servicios/declaracion-anual-personas-fisicas',
    icon: '📊'
  },
  { 
    title: 'Regularización Fiscal', 
    description: 'Pon al día tu situación fiscal',
    url: '/servicios/regularizacion-fiscal',
    icon: '⚖️'
  }
];
---

<Layout title="Página no encontrada - Todoconta" description="La página que buscas no existe. Te ayudamos a encontrar lo que necesitas.">
  <Navbar links={menuLinks} />
  
  <main class="error-page">
    <div class="error-container">
      <div class="error-header">
        <h1 class="error-title">404</h1>
        <p class="error-subtitle">¡Ups! La página que buscas no se encuentra</p>
        <p class="error-description">
          No te preocupes, esto puede pasar cuando se mueven páginas o hay enlaces rotos. 
          Te ayudamos a encontrar lo que necesitas.
        </p>
      </div>

      <div class="error-actions">
        <a href="/" class="btn btn-primary">Volver al inicio</a>
        <a href="/servicios" class="btn btn-secondary">Ver todos los servicios</a>
      </div>

      <div class="services-section">
        <h2 class="services-title">¿Buscas alguno de estos servicios?</h2>
        <div class="services-grid">
          {serviciosPopulares.map(servicio => (
            <a href={servicio.url} class="service-card">
              <div class="service-icon">{servicio.icon}</div>
              <h3 class="service-name">{servicio.title}</h3>
              <p class="service-description">{servicio.description}</p>
            </a>
          ))}
        </div>
      </div>

      <div class="contact-section">
        <h2 class="contact-title">¿No encuentras lo que buscas?</h2>
        <p class="contact-description">
          Contáctame directamente y te ayudo a encontrar la solución que necesitas
        </p>
        <div class="contact-actions">
          <a href="mailto:contacto@todoconta.com" class="btn btn-outline">
            📧 Enviar email
          </a>
          <a href="https://wa.me/5215544753602" class="btn btn-outline">
            💬 WhatsApp
          </a>
        </div>
      </div>
    </div>
  </main>

</Layout>

<style>
  .error-page {
    min-height: calc(100vh - 200px);
    background: var(--color-background);
    padding: var(--space-8) var(--space-4);
  }

  .error-container {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .error-header {
    margin-bottom: var(--space-12);
  }

  .error-title {
    font-size: var(--font-size-7xl);
    font-weight: var(--font-weight-extrabold);
    color: var(--color-primary);
    margin: 0;
    line-height: 1;
  }

  .error-subtitle {
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin: var(--space-4) 0 var(--space-2) 0;
  }

  .error-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .error-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    margin-bottom: var(--space-16);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--border-radius-lg);
    font-weight: var(--font-weight-medium);
    text-decoration: none;
    transition: all var(--transition-normal);
    border: var(--border-width-normal) solid transparent;
    cursor: pointer;
    font-size: var(--font-size-base);
  }

  .btn-primary {
    background: var(--color-primary);
    color: var(--color-white);
  }

  .btn-primary:hover {
    background: var(--color-primary-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .btn-secondary {
    background: var(--color-background-alt);
    color: var(--color-text-primary);
    border-color: var(--color-border);
  }

  .btn-secondary:hover {
    background: var(--color-white);
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .btn-outline {
    background: transparent;
    color: var(--color-text-primary);
    border-color: var(--color-border);
  }

  .btn-outline:hover {
    background: var(--color-primary-light);
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .services-section {
    margin-bottom: var(--space-16);
    padding: var(--space-8);
    background: var(--color-white);
    border-radius: var(--border-radius-xl);
    box-shadow: var(--shadow-sm);
  }

  .services-title {
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-6);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-6);
    margin-top: var(--space-6);
  }

  .service-card {
    display: block;
    padding: var(--space-6);
    background: var(--color-background-alt);
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-normal);
    border: var(--border-width-thin) solid var(--color-border-light);
  }

  .service-card:hover {
    background: var(--color-white);
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-primary);
  }

  .service-icon {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--space-3);
  }

  .service-name {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin: var(--space-2) 0;
  }

  .service-description {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    line-height: 1.5;
  }

  .contact-section {
    padding: var(--space-8);
    background: var(--color-primary-light);
    border-radius: var(--border-radius-xl);
    border: var(--border-width-thin) solid var(--color-border-light);
  }

  .contact-title {
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-3);
  }

  .contact-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-6);
    line-height: 1.6;
  }

  .contact-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .error-title {
      font-size: var(--font-size-6xl);
    }

    .error-actions {
      flex-direction: column;
      align-items: center;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .contact-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
